# TODO

## База данных

### Удаление поля revision из хранилища

**Статус:** Ожидает выполнения

**Описание:**
Необходимо удалить колонку `revision` из таблицы `tasks` в базе данных, так как ревизии больше не используются в системе. Конфликты разрешаются по времени обновления (`updated_at`), а не по ревизиям.

**Задачи:**
1. Создать миграцию Alembic для удаления колонки `revision` из таблицы `tasks`
2. Удалить колонку `revision` из таблицы `tasks_cache` в Android Room Database (требуется миграция базы данных)
3. Удалить колонку `revision` из таблицы `sync_queue` в Android Room Database (если она там есть)
4. Протестировать миграцию на тестовых данных
5. Обновить документацию API (openapi_0_2.yaml) - убрать упоминания revision

**Примечания:**
- Код уже очищен от использования revision
- Миграция должна быть обратно совместимой (если возможно) или требовать ручного обновления данных
- После миграции нужно убедиться, что все клиенты обновлены до версии без revision
