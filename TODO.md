# TODO

## База данных

### Удаление поля revision из хранилища

**Статус:** ✅ Завершено

**Описание:**
Удалена колонка `revision` из таблицы `tasks` в базе данных, так как ревизии больше не используются в системе. Конфликты разрешаются по времени обновления (`updated_at`), а не по ревизиям.

**Выполненные задачи:**
1. ✅ Создана миграция Alembic для удаления колонки `revision` из таблицы `tasks` (ревизия: `9215f41a8633`)
2. ✅ Проверено, что колонка `revision` отсутствует в таблице `tasks_cache` в Android Room Database
3. ✅ Проверено, что колонка `revision` отсутствует в таблице `sync_queue` в Android Room Database
4. ✅ Проверен синтаксис миграции
5. ✅ Проверена документация API (openapi_0_2.yaml) - упоминания revision отсутствуют

**Примечания:**
- Код уже был очищен от использования revision
- Миграция обратно совместима - проверяет наличие колонки перед удалением
- Android код не требует изменений, так как поле revision уже отсутствовало в моделях
- Миграция готова к применению: `alembic upgrade head`
