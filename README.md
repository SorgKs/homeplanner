# HomePlanner - Планировщик и напоминалка для домашнего использования

Приложение для управления домашними задачами и напоминаниями.

## Возможности

- ✅ Напоминание о регулярно повторяющихся задачах
- ✅ Сохранение и напоминание о разовых событиях
- 📱 Простое Android приложение для создания событий и напоминаний
- 🌐 Веб-интерфейс для управления задачами
- 📜 Полная история всех действий с задачами

## Требования

- Python 3.11 или выше
- [uv](https://github.com/astral-sh/uv) - современный менеджер пакетов Python (рекомендуется) или pip

## Установка и развертывание

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd HomePlanner
```

### 2. Установка зависимостей

#### С использованием uv (рекомендуется)

```bash
# Установка uv (если еще не установлен)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Установка зависимостей проекта
uv sync
```

#### С использованием pip

```bash
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения
# На Linux/Mac:
source venv/bin/activate
# На Windows:
venv\Scripts\activate

# Установка зависимостей
pip install -e .
```

### 3. Настройка конфигурации

Создайте файл `.env` в корне проекта (можно скопировать из примера, если есть `.env.example`):

```bash
# База данных
DATABASE_URL=sqlite:///./homeplanner.db
# Для PostgreSQL используйте: postgresql://user:password@localhost/homeplanner

# Безопасность (измените в продакшене!)
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Сервер
HOST=0.0.0.0
PORT=8000
DEBUG=true

# CORS (указать разрешенные домены через запятую)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:8081
```

**Важно**: В продакшене обязательно измените `SECRET_KEY` и установите `DEBUG=false`!

### 4. Инициализация базы данных

База данных создастся автоматически при первом запуске сервера. Если вы используете PostgreSQL, убедитесь, что база данных создана:

```bash
createdb homeplanner  # для PostgreSQL
```

Для применения миграций (если нужно):

```bash
# С uv
uv run alembic upgrade head

# С pip
alembic upgrade head
```

### 5. Запуск серверов

Для работы приложения нужно запустить два сервера: бэкенд (API) и фронтенд (веб-интерфейс).

#### Запуск бэкенда (API)

Бэкенд должен быть доступен по IP-адресу вашей машины в локальной сети, чтобы мобильные устройства могли подключиться.

##### С использованием uv

```bash
uv run python -m backend.main
```

##### С использованием pip

```bash
python -m backend.main
```

Сервер будет доступен по адресу `http://0.0.0.0:8000` (все сетевые интерфейсы)

##### Проверка доступности

Убедитесь, что сервер доступен по IP-адресу вашей машины в локальной сети:
- Узнайте IP-адрес: `ip -4 addr show | grep -E "inet .* (wlan|eth)"`
- Проверьте доступность: `curl http://<ваш-ip>:8000/docs`

#### Запуск фронтенда (веб-интерфейс)

Откройте новый терминал и запустите HTTP-сервер для статических файлов:

```bash
cd frontend
python3 -m http.server 8080
```

Веб-интерфейс будет доступен по адресу `http://localhost:8080` (или `http://<ваш-ip>:8080` из сети)

### 6. Доступ к приложению

- **API документация**: http://localhost:8000/docs (Swagger UI)
- **API документация (альтернативная)**: http://localhost:8000/redoc
- **Веб-интерфейс**: http://localhost:8080 (или http://<ваш-ip>:8080 из сети)

## Настройка веб-интерфейса

Веб-интерфейс находится в папке `frontend/`. Для работы он должен знать адрес API сервера.

### Настройка API Base URL

Веб-интерфейс автоматически определяет адрес API:
1. Из `localStorage` (ключ `apiBaseUrl`) - если был сохранен ранее
2. По умолчанию: `http://192.168.1.2:8000/api/v1`

Для изменения адреса API:
1. Откройте веб-интерфейс
2. Перейдите в **Настройки** (⚙️)
3. В поле "API Base URL (frontend)" укажите правильный адрес (например: `http://192.168.1.10:8000/api/v1`)
4. Нажмите "Сохранить"
5. Обновите страницу

### Настройка CORS

Убедитесь, что в настройках CORS (`CORS_ORIGINS` в `.env`) указан адрес вашего веб-интерфейса:

```bash
CORS_ORIGINS=http://localhost:8080,http://192.168.1.2:8080
```

Замените `192.168.1.2` на IP-адрес вашей машины в локальной сети.

### Реальное время (WebSocket)

- Backend поднимает WebSocket по адресу `ws://<HOST>:8000/ws`.
- Веб‑интерфейс автоматически подключается и применяет входящие события задач (`created`, `updated`, `completed`, `uncompleted`, `shown`, `deleted`) локально без полной перезагрузки.
- Для контроля стабильности отключены любые автоматические массовые вызовы `mark-shown` на рендере — избегайте вызывать этот эндпоинт циклически.

## Развертывание в продакшене

1. Установите `DEBUG=false` в `.env`
2. Измените `SECRET_KEY` на надежный случайный ключ
3. Используйте PostgreSQL вместо SQLite для базы данных
4. Настройте обратный прокси (nginx, Caddy и т.д.) для HTTPS
5. Настройте автоматический запуск сервера через systemd, supervisor или другой процесс-менеджер
6. Настройте регулярное резервное копирование базы данных

### Пример systemd сервиса

Создайте файл `/etc/systemd/system/homeplanner.service`:

```ini
[Unit]
Description=HomePlanner API
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/HomePlanner
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python -m backend.main
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
sudo systemctl daemon-reload
sudo systemctl enable homeplanner
sudo systemctl start homeplanner
```

## База данных

По умолчанию используется SQLite (`homeplanner.db`). База данных создается автоматически при первом запуске.

Для продакшена рекомендуется использовать PostgreSQL. Измените `DATABASE_URL` в `.env`:

```
DATABASE_URL=postgresql://user:password@localhost/homeplanner
```

## Миграции базы данных

Если в проекте есть миграции Alembic, примените их:

```bash
# С uv
uv run alembic upgrade head

# С pip
alembic upgrade head
```

## Android приложение

В репозитории добавлен минимальный Android‑клиент в каталоге `android/` (Kotlin, Jetpack Compose).

### Сборка APK

#### В Android Studio
1. Откройте папку `android/` в Android Studio
2. Выполните синхронизацию Gradle
3. Запустите конфигурацию `app` на эмуляторе API 34 или устройстве

#### Из командной строки

```bash
cd android
./gradlew :app:assembleDebug
```

Собранный APK будет находиться в: `android/app/build/outputs/apk/debug/app-debug.apk`

### Скачивание и установка APK

#### Через веб-интерфейс (QR-код)

1. Запустите бэкенд и фронтенд (см. раздел "Запуск серверов")
2. Откройте веб-интерфейс на `http://<ваш-ip>:8080`
3. Перейдите в **Настройки** (⚙️)
4. В разделе "Установить Android приложение":
   - Укажите хост для телефона: `<ваш-ip>:8000` (например: `192.168.1.10:8000`)
   - **Важно**: не используйте `localhost` — телефон не сможет открыть ссылку
   - Нажмите "Обновить QR"
   - Нажмите "Показать QR для APK"
5. Отсканируйте QR-код телефоном
6. Разрешите установку из неизвестных источников (если требуется)
7. Установите приложение

#### Прямая ссылка

Также можно скачать APK напрямую по ссылке:
- `http://<ваш-ip>:8000/download/latest.apk`
- `http://<ваш-ip>:8000/download/apk`

**Примечание**: APK должен быть собран перед скачиванием (см. раздел "Сборка APK")

### Настройка API Base URL в Android приложении

Приложение по умолчанию ожидает backend по адресу `http://192.168.1.2:8000/api/v1`.

Для изменения адреса:
1. Откройте файл `android/app/build.gradle.kts`
2. Найдите строку с `buildConfigField("String", "API_BASE_URL", ...)`
3. Измените IP-адрес на нужный
4. Пересоберите APK

### Ключевые возможности Android‑клиента

- Нижнее меню: вкладки «Сегодня», «Все задачи», «Настройки», «WS» (лог транзакций).
- Реальное время: клиент подписан на WebSocket и применяет обновления локально.
- Чекбоксы подтверждают/отменяют выполнение через `POST /tasks/{id}/complete` и `POST /tasks/{id}/uncomplete`.
- Отображение времени для каждой задачи и полное название с названием группы.
- Версия приложения: автоматически инкрементируется как `0.1.0.<build_number>` и отображается внизу экрана (см. `BuildConfig.VERSION_NAME`).

Примечание: вкладка «WS» показывает входящие и исходящие сообщения (HTTP/WS) для отладки.

### Советы по отладке

- Если эмулятор не видит backend, проверьте, что используется корректный IP (не `localhost`).
- Для веба убедитесь, что `CORS_ORIGINS` содержит адрес фронтенда (например, `http://192.168.1.2:8080`).
- Если телефон не может скачать APK по QR-коду:
  - Убедитесь, что хост указан правильно (IP-адрес, не `localhost`)
  - Проверьте, что бэкенд запущен и доступен по указанному адресу
  - Проверьте, что APK собран и находится в `android/app/build/outputs/apk/debug/app-debug.apk`
  - Убедитесь, что телефон и сервер в одной сети Wi‑Fi

## Поддержка

Для вопросов по разработке и внутренним деталям проекта см. документацию в папке `docs/`.
