# План рефакторинга WebSocket: перенос из UI в слой синхронизации

> **Статус:** 📋 План создан  
> **Дата создания:** 2025-12-27  
> **Приоритет:** Высокий

## Проблема

В текущей реализации WebSocket подключение управляется в `MainActivity.kt` (UI слой), что нарушает принципы разделения ответственности:

- WebSocket напрямую обновляет UI состояние (`allTasks`)
- WebSocket управляется через `LaunchedEffect` в композабл функции
- Логика обработки событий смешана с UI логикой
- Нет единого механизма обновления данных через локальный кэш

## Цель

Перенести WebSocket в слой синхронизации/сети так, чтобы:

- WebSocket работал независимо от UI
- События сохранялись в локальный кэш через `OfflineRepository`
- UI обновлялся через стандартный механизм наблюдения за кэшем (Flow/State)
- Разделение ответственности: WebSocket → Кэш → UI

## Архитектура после рефакторинга

```
┌─────────────────────────────────────┐
│           UI Layer                   │
│      (MainActivity, Compose)        │
│  - Наблюдает за кэшем (Flow/State)  │
│  - Обновляется автоматически        │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│       Business Logic Layer          │
│         (LocalApi, SyncService)     │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│      Data / Network Layer           │
│  ┌──────────────┐  ┌─────────────┐ │
│  │OfflineRepo.  │  │  ServerApi  │ │
│  └──────────────┘  │   HTTP      │ │
│                    └─────────────┘ │
│  ┌───────────────────────────────┐ │
│  │   WebSocketService            │ │
│  │   - Управление соединением    │ │
│  │   - Обновление кэша           │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

**Поток данных:**
```
Server → WebSocketService → OfflineRepository → Room Database
                                              ↓
UI наблюдает за кэшем (Flow/State) → UI обновляется автоматически
```

---

## Этапы рефакторинга

### Этап 1: Создание WebSocketService

**Задачи:**

1. **Создать новый файл** `android/app/src/main/java/com/homeplanner/sync/WebSocketService.kt`

2. **Определить интерфейс WebSocketService:**
   ```kotlin
   class WebSocketService(
       private val context: Context,
       private val offlineRepository: OfflineRepository,
       private val reminderScheduler: ReminderScheduler,
       private val networkConfig: NetworkConfig
   )
   ```

3. **Реализовать основные методы:**
   - `start()` — запуск WebSocket соединения
   - `stop()` — остановка WebSocket соединения
   - `isConnected(): Boolean` — проверка статуса соединения
   - `private suspend fun connectWebSocket()` — подключение к WebSocket
   - `private suspend fun handleMessage(text: String)` — обработка сообщений

4. **Перенести логику из MainActivity:**
   - Логику подключения/переподключения
   - Обработку событий (task_update, hash_check)
   - Экспоненциальную задержку переподключения
   - Обработку ошибок и закрытия соединения

**Критерии готовности:**
- ✅ WebSocketService создан и компилируется
- ✅ Все методы определены (могут быть заглушками на этом этапе)
- ✅ Класс принимает необходимые зависимости

---

### Этап 2: Реализация обработки событий через OfflineRepository

**Задачи:**

1. **Обновить `handleMessage()` в WebSocketService:**
   - Парсинг сообщений от сервера
   - Обработка событий `task_update` и `hash_check`
   - Обновление локального кэша через `OfflineRepository.saveTasksToCache()`
   - НЕ обновление UI напрямую

2. **Реализовать обработку различных действий:**
   - `created` — сохранение новой задачи в кэш
   - `updated` — обновление задачи в кэше
   - `completed` / `uncompleted` — обновление статуса задачи в кэше
   - `deleted` — удаление задачи из кэша
   - `hash_check` — проверка хеша и при необходимости полная синхронизация

3. **Интеграция с ReminderScheduler:**
   - После обновления задач в кэше вызывать `reminderScheduler.cancelAll()` и `reminderScheduler.scheduleForTasks()`
   - Использовать задачи из кэша, а не из UI состояния

4. **Обработка ошибок:**
   - При ошибке парсинга — логирование, не падение
   - При ошибке сохранения в кэш — логирование, повторная попытка (опционально)

**Зависимости:**
- Методы в `OfflineRepository` (все есть):
  - `saveTasksToCacheLocal(tasks: List<Task>)` — сохранение списка задач ✅
    - Можно использовать для одной задачи: `saveTasksToCacheLocal(listOf(task))`
  - `deleteTaskFromCacheLocal(taskId: Int)` — удаление задачи ✅
  - `getTaskFromCacheLocal(taskId: Int): Task?` — получение задачи ✅
  - `loadTasksFromCacheLocal(): List<Task>` — загрузка всех задач (для ReminderScheduler) ✅

**Критерии готовности:**
- ✅ События обрабатываются и сохраняются в кэш
- ✅ ReminderScheduler вызывается после обновлений
- ✅ Обработка ошибок реализована
- ✅ Логирование всех действий

---

### Этап 3: Управление жизненным циклом WebSocketService

**Задачи:**

1. **Создать метод управления жизненным циклом:**
   - `start()` — запуск WebSocket (с проверкой networkConfig)
   - `stop()` — остановка WebSocket (закрытие соединения)
   - `restart()` — перезапуск (stop + start)

2. **Реализовать управление корутиной:**
   - Использовать `CoroutineScope` для WebSocket корутины
   - Управление жизненным циклом через `start()` / `stop()`
   - Очистка ресурсов при остановке

3. **Интеграция с NetworkConfig:**
   - Автоматический перезапуск при изменении networkConfig
   - Остановка при отсутствии networkConfig
   - Получение WebSocket URL из networkConfig

4. **Экспоненциальная задержка переподключения:**
   - Перенести логику из MainActivity
   - Задержка от 2 секунд до максимум 30 секунд
   - Сброс задержки при успешном подключении

**Критерии готовности:**
- ✅ WebSocket запускается и останавливается корректно
- ✅ Автоматическое переподключение работает
- ✅ Ресурсы освобождаются при остановке
- ✅ Реагирует на изменения networkConfig

---

### Этап 4: Интеграция WebSocketService в MainActivity

**Задачи:**

1. **Создать экземпляр WebSocketService в MainActivity:**
   ```kotlin
   val webSocketService = remember(context, offlineRepository, reminderScheduler, networkConfig) {
       if (networkConfig != null) {
           WebSocketService(context, offlineRepository, reminderScheduler, networkConfig)
       } else {
           null
       }
   }
   ```

2. **Управление жизненным циклом через LaunchedEffect:**
   ```kotlin
   LaunchedEffect(webSocketService) {
       webSocketService?.start()
       try {
           awaitCancellation()
       } finally {
           webSocketService?.stop()
       }
   }
   ```

3. **Удалить старую реализацию WebSocket из MainActivity:**
   - Удалить `LaunchedEffect` с WebSocket логикой
   - Удалить функцию `updateTasksFromEventServer()`
   - Удалить функцию `handleHashMismatchServer()` (перенести в WebSocketService или SyncService)
   - Удалить переменные `wsConnected`, `wsConnecting`
   - Удалить `webSocketUrl` и `resolveWebSocketUrl()`

4. **Обновить UI для отображения статуса WebSocket (опционально):**
   - Если нужно показывать статус WebSocket в UI, добавить метод `isConnected()` в WebSocketService
   - Использовать Flow для наблюдения за статусом (если необходимо)

**Критерии готовности:**
- ✅ WebSocketService интегрирован в MainActivity
- ✅ Старая реализация полностью удалена
- ✅ WebSocket работает и обновляет кэш
- ✅ Приложение компилируется и запускается

---

### Этап 5: Обновление UI для наблюдения за кэшем

**Задачи:**

1. **Проверить текущий механизм наблюдения за кэшем:**
   - Убедиться, что UI загружает задачи из кэша через `OfflineRepository.loadTasksFromCache()`
   - Проверить, что UI обновляется при изменении кэша

2. **Если необходимо, добавить Flow для наблюдения:**
   - В `TaskCacheDao` добавить метод `getAllTasksFlow(): Flow<List<TaskCache>>`
   - В `OfflineRepository` добавить метод `observeTasks(): Flow<List<Task>>`
   - В MainActivity использовать `collectAsState()` для наблюдения за изменениями

3. **Убедиться, что UI обновляется автоматически:**
   - При обновлении задач через WebSocket, UI должен обновляться автоматически
   - Не должно быть необходимости в явных триггерах обновления UI

**Критерии готовности:**
- ✅ UI наблюдает за кэшем через Flow (если необходимо)
- ✅ UI обновляется автоматически при изменениях в кэше
- ✅ Не требуется явных вызовов обновления UI после WebSocket событий

---

### Этап 6: Тестирование и проверка

**Задачи:**

1. **Функциональное тестирование:**
   - Проверить работу WebSocket при различных сценариях:
     - Создание задачи на сервере → обновление в UI
     - Обновление задачи на сервере → обновление в UI
     - Удаление задачи на сервере → удаление из UI
     - Подтверждение/отмена задачи на сервере → обновление в UI
   - Проверить переподключение при разрыве соединения
   - Проверить работу при отсутствии networkConfig

2. **Проверка производительности:**
   - Убедиться, что WebSocket не блокирует UI
   - Проверить использование памяти
   - Проверить батарею (если возможно)

3. **Проверка офлайн режима:**
   - Убедиться, что WebSocket корректно останавливается в офлайне
   - Проверить, что при восстановлении соединения WebSocket переподключается

4. **Проверка обработки ошибок:**
   - Невалидные сообщения
   - Ошибки сети
   - Ошибки сохранения в кэш

**Критерии готовности:**
- ✅ Все функциональные тесты пройдены
- ✅ Производительность приемлема
- ✅ Офлайн режим работает корректно
- ✅ Обработка ошибок работает корректно

---

### Этап 7: Очистка и документация

**Задачи:**

1. **Удаление неиспользуемого кода:**
   - Удалить все функции и переменные, связанные со старой реализацией WebSocket
   - Очистить импорты
   - Удалить комментарии, связанные со старой реализацией

2. **Обновление документации:**
   - Обновить `ANDROID_ARCHITECTURE.md` — убрать предупреждения о текущей реализации
   - Добавить описание WebSocketService в документацию
   - Обновить диаграммы архитектуры

3. **Добавление комментариев и docstring:**
   - Добавить KDoc комментарии к WebSocketService
   - Добавить комментарии к основным методам
   - Документировать обработку событий

**Критерии готовности:**
- ✅ Неиспользуемый код удалён
- ✅ Документация обновлена
- ✅ Код задокументирован

---

## Технические детали

### Структура WebSocketService

```kotlin
class WebSocketService(
    private val context: Context,
    private val offlineRepository: OfflineRepository,
    private val reminderScheduler: ReminderScheduler,
    private val networkConfig: NetworkConfig
) {
    private var webSocket: WebSocket? = null
    private var reconnectDelay = 2000L
    private var shouldReconnect = true
    private var scope: CoroutineScope? = null
    
    private val client = OkHttpClient()
    
    suspend fun start() {
        // Запуск WebSocket соединения
    }
    
    suspend fun stop() {
        // Остановка WebSocket соединения
    }
    
    fun isConnected(): Boolean {
        // Проверка статуса соединения
    }
    
    private suspend fun connectWebSocket() {
        // Логика подключения
    }
    
    private suspend fun handleMessage(text: String) {
        // Обработка сообщений и обновление кэша
    }
    
    private suspend fun handleTaskUpdate(action: String, taskJson: JSONObject?, taskId: Int?) {
        // Обработка событий задач
    }
}
```

### Обработка событий

**Событие `task_update` с действием `created` или `updated`:**
```kotlin
1. Парсинг taskJson в Task
2. offlineRepository.saveTasksToCacheLocal(listOf(task))
3. val currentTasks = offlineRepository.loadTasksFromCacheLocal()
4. reminderScheduler.cancelAll(currentTasks)
5. val updatedTasks = offlineRepository.loadTasksFromCacheLocal()
6. reminderScheduler.scheduleForTasks(updatedTasks)
```

**Событие `task_update` с действием `deleted`:**
```kotlin
1. taskId?.let { offlineRepository.deleteTaskFromCacheLocal(it) }
2. val currentTasks = offlineRepository.loadTasksFromCacheLocal()
3. reminderScheduler.cancelAll(currentTasks)
4. val updatedTasks = offlineRepository.loadTasksFromCacheLocal()
5. reminderScheduler.scheduleForTasks(updatedTasks)
```

**Событие `hash_check`:**
```kotlin
1. Загрузка задач из кэша
2. Вычисление хеша кэша
3. Сравнение с serverHash
4. Если не совпадает → полная синхронизация через SyncService
```

### Методы OfflineRepository

Все необходимые методы уже есть в `OfflineRepository`:

```kotlin
// В OfflineRepository.kt
suspend fun saveTasksToCacheLocal(tasks: List<Task>): Result<Unit>
// Использовать для одной задачи: saveTasksToCacheLocal(listOf(task))

suspend fun deleteTaskFromCacheLocal(id: Int)
suspend fun getTaskFromCacheLocal(id: Int): Task?
suspend fun loadTasksFromCacheLocal(): List<Task>
```

**Примечание:** Для сохранения одной задачи можно использовать `saveTasksToCacheLocal(listOf(task))`, так как `insertTasks()` использует `OnConflictStrategy.REPLACE`, что означает, что задача будет обновлена, если она уже существует, или создана, если её нет.

---

## Риски и меры по их снижению

### Риск 1: Потеря событий при переходе

**Мера:** Тестирование всех сценариев переключения между вкладками и состояниями приложения

### Риск 2: Утечка памяти при неправильном управлении жизненным циклом

**Мера:** 
- Использовать правильный CoroutineScope
- Убедиться, что WebSocket закрывается при остановке
- Тестирование с профилировщиком памяти

### Риск 3: UI не обновляется автоматически

**Мера:**
- Использовать Flow для наблюдения за кэшем
- Проверить, что Compose правильно реагирует на изменения состояния

### Риск 4: Конфликты при одновременном обновлении через WebSocket и синхронизацию

**Мера:**
- Сервер является источником истины
- Все обновления проходят через OfflineRepository
- Последнее обновление выигрывает (по updated_at)

---

## Порядок выполнения

Рекомендуемый порядок выполнения этапов:

1. **Этап 1** — Создание структуры WebSocketService (основа)
2. **Этап 2** — Реализация обработки событий (основная логика)
3. **Этап 3** — Управление жизненным циклом (стабильность)
4. **Этап 4** — Интеграция в MainActivity (интеграция)
5. **Этап 5** — Обновление UI (наблюдение)
6. **Этап 6** — Тестирование (проверка)
7. **Этап 7** — Очистка (финализация)

Каждый этап можно выполнять последовательно, тестируя после каждого.

---

## Критерии успешного завершения

- ✅ WebSocket работает в слое синхронизации/сети
- ✅ События сохраняются в локальный кэш через OfflineRepository
- ✅ UI обновляется автоматически через наблюдение за кэшем
- ✅ Нет прямой связи между WebSocket и UI
- ✅ Код чистый, задокументированный
- ✅ Все тесты пройдены
- ✅ Документация обновлена

---

## Дополнительные улучшения (опционально)

После завершения основного рефакторинга можно рассмотреть:

1. **Статистика WebSocket:**
   - Счетчик полученных сообщений
   - Счетчик ошибок
   - Время последнего подключения

2. **Настройки WebSocket:**
   - Настройка интервала переподключения
   - Настройка таймаутов

3. **Логирование:**
   - Подробное логирование событий WebSocket
   - Метрики производительности

4. **Тесты:**
   - Unit тесты для WebSocketService
   - Интеграционные тесты для проверки обновления кэша

---

**Последнее обновление:** 2025-12-27

