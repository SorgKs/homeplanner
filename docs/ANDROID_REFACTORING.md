# Рефакторинг Android приложения - Новая архитектура на 4 частях

## Обзор

Этот документ описывает комплексный рефакторинг Android приложения HomePlanner для перехода на новую архитектуру, разделённую на 4 основные части: UI, SyncServer, Бизнес логика и Логирование. Рефакторинг направлен на разделение ответственности, улучшение тестируемости и поддерживаемости кода в соответствии с принципами offline-first архитектуры проекта.

## Новая архитектура

**ВАЖНО: ОБЩЕНИЕ С СЕРВЕРОМ ТОЛЬКО В NETWORK LAYER**

Android приложение HomePlanner рефакторится для соответствия новой архитектуре, разделённой на 4 основные части:

1. **UI** (Пользовательский интерфейс)
   - Берет данные из LocalCache и отображает интерфейс
   - Модифицирует LocalCache в соответствии с действиями пользователя
   - Никоим образом не вмешивается в общение с сервером

2. **Business Logic Layer** (Бизнес логика)
    - Мониторит реальное время и настраивает будильники
    - Выполняет пересчет задач (повторяющиеся, дедлайны)
    - Управляет логикой приложения

3. **Data Layer** (Слой данных)
    - LocalApi — работа с локальным хранилищем для UI и Business Logic
    - OfflineRepository — низкоуровневая работа с базой данных
4. **Network Layer** (Сетевой слой)
    - **ОБЩЕНИЕ С СЕРВЕРОМ ТОЛЬКО ЗДЕСЬ**
    - ServerApi — HTTP запросы к серверу
    - WebSocket сервис — получение обновлений в реальном времени
    - Обработка сетевых запросов и ошибок

5. **Логирование** (Журналирование)

## Исходные проблемы

### MainActivity.kt (исходная версия)
- **Размер**: 2925 строк кода
- **Ответственности** (нарушение Single Responsibility):
  - Управление жизненным циклом Activity
  - Управление состоянием UI (40+ переменных remember)
  - Бизнес-логика синхронизации задач
  - Логика сетевых запросов
  - Валидация данных
  - Обработка WebSocket событий (⚠️ нарушение архитектуры - WebSocket должен быть в Network Layer)
  - UI рендеринг (Compose компоненты)

### Нарушения принципов SOLID и архитектурных требований
- **Single Responsibility**: Один компонент выполнял функции UI, бизнес-логики и сети
- **Open/Closed**: Трудно расширять без изменения существующего кода
- **Dependency Inversion**: Прямая зависимость от сервисов в UI коде
- **Архитектурные нарушения**: WebSocket в UI слое вместо Network Layer (см. [ANDROID_ARCHITECTURE.md](ANDROID_ARCHITECTURE.md))

## Соглашения об именовании

В соответствии с [RULES.md](RULES.md), для всех функций, общающихся с локальным кешем или сервером, обязательно используются суффиксы:

- **`Local`** - для методов, работающих с локальным кэшем (оптимистичные обновления)
  - `createTaskLocal()`, `updateTaskLocal()`, `completeTaskLocal()`, `deleteTaskLocal()`

- **`Server`** - для методов, выполняющих запросы к серверу
  - `syncTasksServer()`, `syncGroupsAndUsersServer()`

Это соглашение обеспечивает четкое понимание ответственности каждого метода и предотвращает смешивание локальной и серверной логики.

## Соответствие требованиям проекта

### Архитектурные принципы (из [RULES.md](RULES.md))
- ✅ **Чёткое разделение на слои**: UI Layer, Business Logic Layer, Data Layer, Network Layer
- ✅ **Модульная архитектура**: Каждый компонент имеет чёткую ответственность
- ✅ **Документирование**: Подробная документация архитектуры и компонентов

### Offline-First архитектура (из [OFFLINE_FIRST_ARCHITECTURE.md](OFFLINE_FIRST_ARCHITECTURE.md))
- ✅ **Сервер — источник истины**: Все конфликты разрешаются на сервере
- ✅ **Локальный кэш — источник правды для UI**: UI загружается из кэша
- ✅ **Оптимистичные обновления**: Действия применяются локально немедленно
- ✅ **Фоновая синхронизация**: SyncService работает асинхронно

### Android архитектура (из [ANDROID_ARCHITECTURE.md](ANDROID_ARCHITECTURE.md))
- ✅ **Четырехуровневая архитектура**: UI → Business Logic → Data → Network
- ✅ **WebSocket в правильном слое**: Перемещён из UI в Network Layer
- ✅ **Чёткое разделение ответственности**: LocalApi, SyncService, OfflineRepository

### Технические требования Android (из [ANDROID_GUIDE.md](ANDROID_GUIDE.md))
- ✅ **Технологический стек**: Kotlin, Jetpack Compose, Android SDK 24+
- ✅ **Сборка**: Gradle wrapper, debug/release APK
- ✅ **Версионирование**: MAJOR.MINOR из pyproject.toml, PATCH из android/version.json
- ✅ **Логирование**: Android Log для отладки, структурированные логи для production

### Принципы разработки (из [DEVELOPMENT.md](DEVELOPMENT.md))
- ✅ **Типизация**: Полная типизация Kotlin кода
- ✅ **Документирование**: Docstrings и архитектурная документация
- ✅ **Модульная архитектура**: Чёткое разделение на модели, сервисы, роутеры, утилиты
- ✅ **Тестирование**: Возможность изолированного тестирования компонентов

## Детальная архитектура приложения

### 1. UI Layer

**Расположение**: `MainActivity.kt`, `ui/` (планируется)

**Ответственность**:
- Берет данные из LocalCache и отображает интерфейс
- Модифицирует LocalCache в соответствии с действиями пользователя
- Никоим образом не вмешивается в общение с сервером

**Компоненты**:
```
ui/tasks/ (планируется)
├── TaskListScreen.kt      # Основной экран задач (чистый Compose)
├── TaskItem.kt           # Элемент задачи с swipe действиями
└── TaskDialogs.kt        # Диалоги создания/редактирования

ui/settings/ (планируется)
├── SettingsScreen.kt     # Экран настроек (рефакторинг)
├── NetworkSettings.kt    # Настройки сети
└── UserSettings.kt       # Выбор пользователя
```

**Принципы реализации**:
- **Чистые функции**: Нет бизнес-логики, только отображение состояния
- **State-driven**: Все состояние приходит из Business Logic Layer через StateFlow
- **Event-driven**: Действия пользователя отправляются в Business Logic Layer
- **Composable-first**: Использование Jetpack Compose для декларативного UI

### 2. Business Logic Layer

**Расположение**: `viewmodel/`, `utils/`

**Ответственность**:
- Мониторит реальное время и настраивает будильники
- Выполняет пересчет задач (повторяющиеся, дедлайны)
- Управляет логикой приложения и бизнес-правилами
- Обрабатывает фильтрацию и сортировку задач
- Координирует взаимодействие между UI, Data и Network слоями

**Компоненты**:
```
viewmodel/
├── TaskViewModel.kt      # Управление состоянием задач и координация бизнес-логики
└── SettingsViewModel.kt  # Управление настройками (планируется)

utils/
├── TaskDateCalculator.kt # Вычисление дат для задач
├── TodayTaskFilter.kt    # Фильтрация задач для вкладки "Сегодня"
├── TaskFilterService.kt  # Общая логика фильтрации и сортировки задач (планируется)
└── ReminderScheduler.kt  # Планирование уведомлений
```

#### TaskViewModel.kt
**Ответственность**: Управление состоянием задач и координация бизнес-логики

**Ключевые компоненты**:
- `TaskScreenState`: Immutable состояние экрана задач (соответствует State в MVI паттерне)
- Методы для CRUD операций над задачами
- Интеграция с Network Layer для синхронизации
- Управление жизненным циклом данных через AndroidViewModel
- Реактивные обновления через StateFlow

**Принципы реализации**:
- **Offline-first**: Все операции сначала применяются к локальному кэшу
- **Оптимистичные обновления**: UI обновляется немедленно
- **Фоновая синхронизация**: Network Layer работает асинхронно
- **Обработка ошибок**: Graceful degradation при сетевых проблемах

**Соглашения об именовании** ([RULES.md](RULES.md)):
- Методы, работающие с локальным кэшем: суффикс `Local` (createTaskLocal, updateTaskLocal)
- Методы, работающие с сервером: суффикс `Server` (syncTasksServer, syncGroupsAndUsersServer)

**Пример использования**:
```kotlin
val viewModel = TaskViewModel(application)
viewModel.initialize(networkConfig, apiBaseUrl, selectedUser, taskSyncManager)

// Реактивное наблюдение за состоянием
val state by viewModel.state.collectAsState()

// Работа с локальным кэшем (оптимистичные обновления)
viewModel.createTaskLocal(task, userIds)
viewModel.completeTaskLocal(taskId)

// Синхронизация с сервером
viewModel.syncWithServer()
```

### 3. Data Layer

**Расположение**: `api/`, `repository/`, `database/`

**Ответственность**:
- LocalApi — работа с локальным хранилищем для UI и Business Logic
- OfflineRepository — низкоуровневая работа с базой данных

**Компоненты**:
```
api/
├── LocalApi.kt           # Работа с локальным хранилищем для UI
├── GroupsLocalApi.kt     # Локальный API для групп
└── UsersLocalApi.kt      # Локальный API для пользователей

repository/
└── OfflineRepository.kt  # Репозиторий для работы с локальным хранилищем

database/
├── AppDatabase.kt        # Room Database
├── dao/                  # Data Access Objects
└── entity/               # Entity классы
```

#### LocalApi.kt
**Ответственность**: Предоставление данных из локального хранилища для UI и Business Logic

**Методы**:
```kotlin
suspend fun getTasksLocal(activeOnly: Boolean = true): List<Task>
suspend fun createTaskLocal(task: Task, assignedUserIds: List<Int> = emptyList()): Task
suspend fun updateTaskLocal(taskId: Int, task: Task, assignedUserIds: List<Int> = emptyList()): Task
suspend fun completeTaskLocal(taskId: Int): Task
suspend fun uncompleteTaskLocal(taskId: Int): Task
suspend fun deleteTaskLocal(taskId: Int)
```

**Особенности**:
- Все методы работают только с локальным хранилищем
- Не выполняет запросы к серверу
- Все изменения автоматически добавляются в очередь синхронизации
- Возвращает данные немедленно (оптимистичные обновления)

#### OfflineRepository.kt
**Ответственность**: Низкоуровневая работа с Room database

**Основные методы**:
```kotlin
suspend fun saveTasksToCache(tasks: List<Task>): Result<Unit>
suspend fun loadTasksFromCache(): List<Task>
suspend fun getTaskFromCache(id: Int): Task?
suspend fun deleteTaskFromCache(id: Int)
suspend fun updateRecurringTasksForNewDay(dayStartHour: Int): Boolean
```

### 4. Network Layer

**Расположение**: `api/`, `sync/`, `services/`

**Ответственность**:
- Синхронизация задач с сервером и валидация данных
- Управление очередью синхронизации (отправка операций на сервер)
- ServerApi — HTTP запросы к серверу
- WebSocket сервис — получение обновлений в реальном времени
- Обработка сетевых запросов и ошибок

**Компоненты**:
```
api/
├── ServerApi.kt          # HTTP запросы к серверу
├── GroupsServerApi.kt    # API для групп (сервер)
└── UsersServerApi.kt     # API для пользователей (сервер)

services/
├── TaskSyncManager.kt     # Синхронизация задач с сервером
├── TaskValidationService.kt # Валидация данных задач
└── SyncService.kt        # Управление очередью синхронизации

sync/ (deprecated)
└── (перемещено в services/)
```

#### TaskSyncManager.kt
**Ответственность**: Синхронизация задач между локальным кэшем и сервером

**Архитектурное соответствие**:
- **Data Layer**: Обновляет OfflineRepository
- **Network Layer**: Координирует сетевые запросы через ServerApi

**Функциональность**:
- Полная синхронизация задач с сервером
- Отправка операций на сервер для разрешения конфликтов по timestamp
- Синхронизация справочников (группы, пользователи)
- Управление очередью операций из SyncService

**Ключевые методы**:
- `syncTasksServer()`: Основная синхронизация с retry логикой
- `syncGroupsAndUsersServer()`: Синхронизация метаданных

**Особенности**:
- **Сервер разрешает конфликты**: Все конфликты разрешаются на сервере по timestamp
- **Retry логика**: Экспоненциальная задержка при сетевых ошибках
- **Полная синхронизация**: Отправка всех операций на сервер для обработки

#### ServerApi.kt
**Ответственность**: Выполнение HTTP-запросов к REST API сервера

**Методы**:
```kotlin
fun getTasksServer(activeOnly: Boolean = true): List<Task>
fun createTaskServer(task: Task, assignedUserIds: List<Int> = emptyList()): Task
fun updateTaskServer(taskId: Int, task: Task, assignedUserIds: List<Int> = emptyList()): Task
fun completeTaskServer(taskId: Int): Task
fun uncompleteTaskServer(taskId: Int): Task
fun deleteTaskServer(taskId: Int)
fun syncQueueServer(queueItems: List<SyncQueueItem>): List<Task>
```

**Особенности**:
- Не содержит логики кэширования
- Не зависит от наличия интернета (выбрасывает исключения при отсутствии)
- Используется только TaskSyncManager для синхронизации

#### TaskValidationService.kt
**Ответственность**: Валидация данных задач перед отправкой на сервер

**Функциональность**:
- Проверка корректности данных задачи
- Валидация бизнес-правил
- Предварительная проверка перед сетевыми запросами

#### SyncService.kt
**Ответственность**: Управление очередью синхронизации и отправка операций на сервер

**Функциональность**:
- Добавление операций в очередь синхронизации
- Получение операций из очереди
- Отправка операций на сервер через ServerApi
- Обработка ответов сервера и очистка очереди
- Retry логика при сетевых ошибках

#### WebSocket сервис
**Ответственность**: Получение обновлений в реальном времени

**Особенности**:
- Автоматическое переподключение
- Обработка сообщений о событиях задач
- Обновление локального кэша при получении событий
- UI обновляется через наблюдение за кэшем

**События**:
- `task_update` — обновление задачи
- `task_created` — создание задачи
- `task_deleted` — удаление задачи
- `task_completed` — подтверждение задачи
- `task_uncompleted` — отмена подтверждения

#### Логика статуса соединения

**Ответственность**: Определение состояния сетевого соединения на основе реальных запросов

**Архитектурное соответствие**:
- **Network Layer**: Определение статуса основано исключительно на результатах HTTP запросов
- **Offline-first принцип**: Не зависит от ConnectivityManager, работает даже без интернета

**Функциональность**:
- Отслеживание успешности реальных запросов к серверу
- Классификация ошибок по степени критичности
- Таймауты для предотвращения ложных "ONLINE" статусов
- Определение статуса только по факту коммуникации с сервером

**Состояния соединения** (соответствует требованиям offline-first):
- `UNKNOWN`: Начальное состояние при запуске приложения
- `ONLINE`: Есть успешная попытка коммуникации в последние 5 минут
- `DEGRADED`: Есть неуспешные попытки, но менее 5 подряд
- `OFFLINE`: 5+ неуспешных попыток подряд ИЛИ превышен таймаут (5 мин)

**Логика классификации ошибок**:
- **Критические ошибки** (переводят в OFFLINE):
  - HTTP 500 (Internal Server Error)
  - Таймауты подключения (>30 сек)
  - Сетевые ошибки (Connection refused, No route to host, etc.)
  - SSL ошибки, UnknownHostException
- **Некритические ошибки** (сервер ответил, статус сохраняется):
  - HTTP 4xx (Bad Request, Unauthorized, etc.)
  - Любые другие коды ответа от сервера

**Преимущества подхода**:
- **Реальная проверка**: Статус отражает способность общаться с сервером
- **Локальный сервер**: Работает даже без интернета (localhost/127.0.0.1)
- **Оптимизация**: Не тратит батарею на постоянный мониторинг сети
- **Точность**: Избегает ложных "OFFLINE" при работе через локальную сеть

## Архитектурные преимущества

### 1. Разделение ответственности
- **ViewModel**: Состояние и бизнес-логика
- **Services**: Инфраструктурная логика
- **UI Components**: Чистые composable функции

### 2. Тестируемость
- ViewModel можно тестировать без UI
- Сервисы тестируются изолированно
- UI компоненты тестируются с mock данными

### 3. Поддерживаемость
- Изменения в синхронизации не затрагивают UI
- Новые функции добавляются модульно
- Ошибки локализованы в конкретных компонентах

### 4. Реактивность
- StateFlow для реактивных обновлений
- Coroutines для асинхронных операций
- Lifecycle-aware компоненты

## Миграционный план

### Фаза 1: Инфраструктура ✅

**Цель**: Создать фундамент для новой архитектуры
**Соответствие**: [ANDROID_ARCHITECTURE.md](ANDROID_ARCHITECTURE.md) - разделение на слои

- [x] **Создать TaskViewModel** - Управление состоянием задач (Business Logic Layer)
- [x] **Создать TaskSyncManager** - Синхронизация с сервером (Network Layer)
- [x] **Создать TaskValidationService** - Валидация данных (Network Layer)
- [x] **Упростить архитектуру статуса соединения** - Определение только по реальным запросам
- [x] **Настроить StateFlow** - Реактивные обновления состояния
- [x] **Интегрировать с существующими сервисами** - LocalApi, SyncService, OfflineRepository

### Фаза 2: UI компоненты 🔄

**Цель**: Разделить монолитный TasksScreen на модульные компоненты
**Соответствие**: [ANDROID_ARCHITECTURE.md](ANDROID_ARCHITECTURE.md) - UI Layer

- [ ] **Создать TaskListScreen.kt** - Основной экран с LazyColumn
- [ ] **Создать TaskItem.kt** - Элемент списка с swipe действиями
- [ ] **Создать TaskDialogs.kt** - Диалоги создания/редактирования/удаления
- [ ] **Интегрировать фильтрацию** - Использовать TodayTaskFilter из Business Logic Layer
- [ ] **Вынести константы** - TaskConstants.kt для строк и размеров

### Фаза 3: Интеграция и рефакторинг 🔄

**Цель**: Интегрировать новые компоненты в MainActivity
**Соответствие**: [OFFLINE_FIRST_ARCHITECTURE.md](OFFLINE_FIRST_ARCHITECTURE.md) - оптимистичные обновления

- [ ] **Рефакторить MainActivity** - Использовать TaskViewModel вместо remember переменных
- [ ] **Заменить TasksScreen** - На композицию из TaskListScreen + TaskDialogs
- [ ] **Обновить WebSocket обработку** - Переместить в TaskSyncManager (соответствие архитектуре)
- [ ] **Протестировать основные функции** - CRUD операции, синхронизация, офлайн режим

### Фаза 4: SettingsScreen и оптимизация 🔄

**Цель**: Рефакторить оставшиеся компоненты и оптимизировать
**Соответствие**: [ANDROID_GUIDE.md](ANDROID_GUIDE.md) - технические требования

- [ ] **Создать SettingsViewModel** - Логика настроек в отдельном ViewModel
- [ ] **Рефакторить SettingsScreen** - Разделить на компоненты
- [ ] **Добавить unit тесты** - Тестирование ViewModel и сервисов
- [ ] **Оптимизировать производительность** - StateFlow, recomposition
- [ ] **Добавить логирование** - Структурированные логи по [LOGGING_FORMAT.md](LOGGING_FORMAT.md)

## Технические детали

### State Management
```kotlin
data class TaskScreenState(
    val allTasks: List<Task> = emptyList(),
    val isLoading: Boolean = false,
    val error: String? = null,
    // ... другие поля
)

class TaskViewModel : AndroidViewModel() {
    private val _state = MutableStateFlow(TaskScreenState())
    val state: StateFlow<TaskScreenState> = _state.asStateFlow()
}
```

### Dependency Injection
Текущая реализация использует ручное создание зависимостей:
```kotlin
val viewModel = TaskViewModel(application)
viewModel.initialize(networkConfig, apiBaseUrl, selectedUser)
```

Рекомендуется внедрение Hilt/Koin для будущих улучшений.

### Error Handling
- Критические ошибки (сеть): Mark failed request → OFFLINE статус
- Некритические ошибки (сервер ответил): Continue with current status
- Логика встроена в TasksScreen с классификацией isCriticalError()

## Метрики успеха

### До рефакторинга:
- MainActivity.kt: 2925 строк
- Количество ответственностей: 8+
- Тестируемость: Низкая
- Поддерживаемость: Низкая

### После рефакторинга (цель):
- MainActivity.kt: ~200 строк (только Activity lifecycle)
- TasksScreen.kt: ~300 строк (только UI composition)
- Количество файлов: 15+ (модульная структура)
- Тестируемость: Высокая (каждый компонент тестируется отдельно)
- Поддерживаемость: Высокая (изменения изолированы)

## Риски и mitigation

### 1. Риск регрессии
**Mitigation**: Постепенная миграция с параллельным запуском старого и нового кода

### 2. Performance impact
**Mitigation**: Профилирование и оптимизация StateFlow, избегание unnecessary recompositions

### 3. Learning curve
**Mitigation**: Детальная документация, обучение команды новым паттернам

## Следующие шаги

1. **Завершить UI компоненты** - разделить TasksScreen на мелкие composables
2. **Интегрировать ViewModel** - заменить remember логику на реактивную
3. **Добавить тесты** - unit тесты для ViewModel и сервисов
4. **Оптимизировать** - performance profiling и улучшения

## Риски и mitigation

### 1. **Риск регрессии функциональности**
**Mitigation**:
- Поэтапная миграция с параллельным запуском старого и нового кода
- Обширное тестирование каждой фазы
- Rollback план при обнаружении критических ошибок

### 2. **Performance degradation**
**Mitigation**:
- Профилирование StateFlow и recomposition
- Оптимизация больших списков через LazyColumn
- Избегание unnecessary state updates

### 3. **Learning curve для команды**
**Mitigation**:
- Детальная документация архитектуры
- Code examples для новых паттернов
- Pair programming при внедрении

## Метрики успеха

### До рефакторинга:
- MainActivity.kt: 2925 строк
- Количество ответственностей: 8+
- Тестируемость: Низкая
- Архитектурные нарушения: WebSocket в UI слое
- Время на добавление функции: Высокое

### После рефакторинга (цель):
- MainActivity.kt: ~200 строк (только Activity lifecycle)
- TasksScreen.kt: ~300 строк (только UI composition)
- Количество файлов: 15+ (модульная архитектура)
- Тестируемость: Высокая (каждый компонент тестируется отдельно)
- Архитектурное соответствие: 100%
- Время на добавление функции: Низкое

## Следующие шаги

### Немедленные действия:
1. **Завершить Фазу 2** - Разделить TasksScreen на компоненты
2. **Интегрировать TaskViewModel** - Обновить MainActivity
3. **Переместить WebSocket** - Из UI в TaskSyncManager

### Среднесрочные цели:
1. **Добавить unit тесты** - ViewModel, сервисы
2. **Рефакторить SettingsScreen** - Аналогичный подход
3. **Оптимизировать производительность** - Профилирование

### Долгосрочные улучшения:
1. **Добавить dependency injection** - Hilt/Koin
2. **Внедрить MVI паттерн** - Mavericks/Orbit
3. **Добавить integration тесты** - Android Instrumentation

## Документация и обучение

### Для разработчиков:
- **Архитектурные решения** - Обоснование каждого компонента
- **Code examples** - Примеры использования новых компонентов
- **Migration guide** - Пошаговое руководство по миграции

### Для QA:
- **Тестовые сценарии** - Проверка offline-first поведения
- **Regression testing** - Критические пути приложения

## Заключение

Рефакторинг MainActivity.kt представляет собой фундаментальное улучшение архитектуры Android приложения HomePlanner, обеспечивающее:

- **📏 Масштабируемость**: Легко добавлять новые функции без нарушения существующего кода
- **🔒 Надежность**: Лучшая обработка ошибок и состояний, graceful degradation
- **🛠️ Поддерживаемость**: Четкое разделение ответственности, модульная архитектура
- **🧪 Тестируемость**: Возможность изолированного тестирования компонентов
- **📚 Соответствие требованиям**: Полное соблюдение архитектурных принципов проекта

Новая архитектура следует:
- **Android архитектуре** из [ANDROID_ARCHITECTURE.md](ANDROID_ARCHITECTURE.md)
- **Offline-first принципам** из [OFFLINE_FIRST_ARCHITECTURE.md](OFFLINE_FIRST_ARCHITECTURE.md)
- **Правилам проекта** из [RULES.md](RULES.md)
- **Современным Android паттернам** (MVVM, Clean Architecture, StateFlow)

**Статус**: Активная разработка (Фаза 1 завершена, инфраструктура готова)
**Следующий этап**: Фаза 2 - Разделение UI компонентов

---

**Статус документа**: Активный (обновляется по мере прогресса рефакторинга)
**Последнее обновление**: 2025-12-29
**Ответственный**: Архитектор проекта

## Недавние изменения
- **2025-12-29**: Упрощена архитектура статуса соединения - убрана зависимость от ConnectivityManager, статус определяется только по результатам реальных HTTP запросов (ConnectionMonitor → встроенная логика)