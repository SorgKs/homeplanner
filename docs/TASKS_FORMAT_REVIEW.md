# Оценка документации TASKS_FORMAT.md как требований к новой версии

## Введение
Документ описывает требования к новой версии системы. Оценка проводится с точки зрения:
- Чёткости и однозначности требований
- Согласованности между требованиями
- Полноты для реализации
- Реализуемости

---

## ✅ Сильные стороны документации

1. **Хорошо структурирован** — разделы по БД, бэкенду, веб и Android клиентам
2. **Детально описаны UI компоненты** — модальные окна, кнопки, свайпы
3. **Отлично описан механизм оповещений** — Android-клиент, откладывание, разрешения
4. **Чётко описаны форматы данных** — JSON примеры для API
5. **Есть план миграций** — понимание перехода от текущего состояния

---

## 🔴 Критические проблемы в требованиях

### 1. Неясность: что использовать для поля "Дата/время начала" после удаления `next_due_date`

**Проблема:**
- **Требование** (строка 414): "Удалить `next_due_date`"
- **Описание** (строка 127, веб): "Дата/время начала: `task.next_due_date`" — в разделе "Заполнение при редактировании"
- **Описание** (строка 243, Android): "Дата/время начала: `task.nextDueDate`" — в разделе "Заполнение при редактировании"
- **В разделе БД** (строка 10-24): поле `next_due_date` отсутствует

**Оценка:**
- В разделе БД поле `next_due_date` не указано (правильно, т.к. оно должно быть удалено)
- Но в описании "Заполнение при редактировании" указано, что для поля "Дата/время начала" берётся `task.next_due_date`
- После миграции поле `next_due_date` не будет существовать, поэтому это описание станет неактуальным

**Рекомендация:**
- Обновить описание "Заполнение при редактировании" после миграции:
  - **Для веб-версии** (строка 127): "Дата/время начала: `task.reminder_time` (если есть, иначе текущая дата/время)"
  - **Для Android-клиента** (строка 243): "Дата/время начала: `task.reminderTime` (если есть, иначе текущая дата/время)"
- Или: убрать поле "Дата/время начала" из формы, если оно не нужно (использовать только "Время напоминания")

**Вывод:** Описание формы редактирования нужно обновить после миграции, чтобы соответствовать новой структуре БД.

---

### 2. Неясность: описание смешивает текущее состояние и требования к новой версии

**Проблема:**
- **Описание** (строка 43): "Валидатор `ensure_reminder_time`: если `reminder_time is None`, подставляется `next_due_date`"
- **Описание** (строка 47): "При `create_task`/`update_task` сервер дополнительно убеждается, что `reminder_time` установлен (подставляет `next_due_date`, если пусто)"
- **Требование** (строка 79): "поле `reminder_time` ОБЯЗАТЕЛЬНО при создании. Если не передано или пусто — ошибка 400"
- **Требование** (строка 81): "Никаких автоподстановок или фоллбеков. При отсутствии любого обязательного поля (включая `reminder_time`) — ошибка 400"

**Оценка:**
- Строки 43 и 47 описывают валидатор, который подставляет `next_due_date` если `reminder_time` пусто
- Строки 79 и 81 требуют ошибку 400 и запрещают автоподстановки
- После миграции `next_due_date` не будет существовать, поэтому валидатор не сможет подставлять `next_due_date`
- Строки 43 и 47, вероятно, описывают ТЕКУЩЕЕ состояние (до миграции), а строки 79 и 81 — требования к НОВОЙ версии (после миграции), но это не указано явно

**Рекомендация:**
- Уточнить строки 43 и 47: "В текущей версии (до миграции) валидатор `ensure_reminder_time` подставляет `next_due_date`, если `reminder_time is None`. После миграции валидатор будет удалён, и `reminder_time` станет обязательным без фоллбеков."
- Или: Удалить строки 43 и 47, если они описывают только текущее состояние, и оставить только требования к новой версии (строки 79 и 81)

**Вывод:** Описание смешивает текущее состояние и требования к новой версии, нужна доработка для ясности.

---

### 3. Неясность: `completed` vs `last_completed_at`

**Проблема:**
- **Требование** (строка 21, БД): "`completed: BOOLEAN NOT NULL DEFAULT FALSE`"
- **Требование** (строка 417, миграции): "Заменить `last_completed_at` на `completed`"
- **Текущий код**: использует `last_completed_at`

**Оценка:**
- Требование ясное: нужно заменить `last_completed_at` на `completed`
- Но нет описания, как обрабатывать существующие данные `last_completed_at` при миграции

**Рекомендация:**
- Добавить в миграцию (пункт 3): "Если `last_completed_at` не NULL и дата совпадает с сегодняшним днём, установить `completed = TRUE`, иначе `completed = FALSE`"

**Вывод:** Требование ясное, но неполное (нет описания миграции данных).

---

## 🟡 Важные проблемы в требованиях

### 4. Отсутствует описание расчёта следующего наступления

**Проблема:**
- **Требование** (строка 30): "Расчёт следующего наступления выполняется от текущего `reminder_time` и правил повторения"
- **Но нет описания:**
  - Как именно рассчитывается для `recurring` (daily/weekly/monthly/yearly)?
  - Как рассчитывается для `interval` (от даты выполнения или от `reminder_time`)?
  - Что происходит с `one_time` задачами после выполнения?
  - Как влияет `anchor_weekday` на расчёт?

**Оценка:**
- Требование слишком общее, не реализуемо без дополнительных деталей

**Рекомендация:**
Добавить раздел "Расчёт следующего наступления":
```
### Расчёт следующего наступления задачи

После выполнения задачи (`completed = TRUE`) сервер рассчитывает следующее `reminder_time`:

1. **`one_time` задачи:**
   - После выполнения задача не повторяется
   - `reminder_time` не изменяется (или устанавливается в NULL?)

2. **`recurring` задачи:**
   - Расчёт от текущего `reminder_time` + правила повторения:
     - `daily`: следующий день в то же время
     - `weekly`: +7 дней (или +N*7 дней, если `recurrence_interval > 1`)
     - `monthly`: +1 месяц (или +N месяцев)
     - `yearly`: +1 год (или +N лет)
   - Если указан `anchor_weekday`:
     - Для `weekly`: следующий указанный день недели
     - Для `monthly`/`yearly`: следующий указанный день недели в месяце/годе

3. **`interval` задачи:**
   - Расчёт от даты выполнения (`completed` = TRUE) + `interval_days`
   - Если задача выполнена сегодня, `reminder_time` = сегодня + `interval_days` дней в то же время
```

**Вывод:** Требование неполное, критично для реализации.

---

### 5. Отсутствует описание временных зон

**Проблема:**
- **Требование** (строка 398): "Формат дат: ISO 8601 локальный"
- **Требование** (строка 28): "при наступлении нового дня сервер сбрасывает `completed = FALSE`"
- **Но не описано:**
  - В какой временной зоне работает сервер?
  - Как клиенты должны обрабатывать временные зоны?
  - Как определяется "новый день" (по UTC или локальному времени сервера)?

**Оценка:**
- Требование критично для корректной работы сброса `completed`
- Неясно, как синхронизировать время между клиентами в разных часовых поясах

**Рекомендация:**
Добавить раздел "Временные зоны":
```
### Временные зоны

- Сервер работает в временной зоне, указанной в `TIMEZONE` (по умолчанию `Europe/Moscow`)
- Все даты в БД хранятся в локальном времени сервера (без информации о временной зоне)
- Клиенты должны отправлять даты в ISO 8601 формате без информации о временной зоне
- "Новый день" определяется по локальному времени сервера
- При сбросе `completed = FALSE` сервер проверяет, наступил ли новый день по локальному времени
```

**Вывод:** Требование неполное, критично для корректной работы.

---

### 6. Неясность: что происходит с `next_due_date` после миграции

**Проблема:**
- **Требование** (строка 414): "Удалить `next_due_date`"
- **Но не описано:**
  - Что использовать вместо `next_due_date` для фильтрации "сегодня"?
  - Как определить, просрочена ли задача?
  - Что использовать для отображения даты выполнения в UI?

**Оценка:**
- Требование неполное: описано ЧТО удалить, но не описано, ЧТО использовать вместо

**Рекомендация:**
- Уточнить: "После удаления `next_due_date` использовать `reminder_time` для всех операций: фильтрация 'сегодня', определение просроченных задач, отображение даты выполнения"
- Или: "Оставить `next_due_date` как отдельное поле для даты выполнения, `reminder_time` — только для времени уведомления"

**Вывод:** Требование неполное, нужна доработка.

---

### 7. Неясность: формат запроса при откладывании оповещения

**Проблема:**
- **Требование** (строка 339): "Отправляется запрос `PUT /api/v1/tasks/{taskId}` с обновлённым `reminder_time`"
- **Но не описано:**
  - Передаётся только `reminder_time` или весь объект задачи?
  - Какие другие поля должны быть переданы?

**Оценка:**
- Требование неполное: описан факт отправки запроса, но не формат

**Рекомендация:**
Уточнить в разделе "Механизм откладывания оповещения":
```
5. Если `taskId` известен (не равен -1), выполняется обновление `reminder_time` задачи на сервере:
   - Отправляется запрос `PUT /api/v1/tasks/{taskId}` с ПОЛНЫМ объектом задачи:
     - Все поля задачи передаются как есть
     - `reminder_time` обновляется на текущее время + задержка
     - Формат соответствует разделу "Обновление задачи" (строка 262)
```

**Вывод:** Требование неполное, но легко дополняется.

---

## 🟢 Средние проблемы в требованиях

### 8. Отсутствует описание обработки ошибок

**Проблема:**
- Упоминаются коды ошибок (400), но нет систематического описания

**Рекомендация:**
Добавить раздел "Обработка ошибок" с описанием всех возможных ошибок и поведения клиентов.

---

### 9. Неполное описание WebSocket

**Проблема:**
- Описаны события, но нет описания подключения, переподключения, обработки ошибок

**Рекомендация:**
Добавить раздел "WebSocket соединение" с описанием URL, механизма переподключения, формата сообщений.

---

### 10. Неясность: `weekdays`/`weekends` в `recurrence_type` vs `anchor_weekday`

**Проблема:**
- **Требование** (строка 16): `recurrence_type` может быть `weekdays` или `weekends`
- **Требование** (строка 23): `anchor_weekday` может быть 8 (будни) или 9 (выходные)
- **Неясно:** как связаны эти два поля?

**Рекомендация:**
Уточнить:
- Вариант 1: `recurrence_type: "weekdays"/"weekends"` заменяет использование `anchor_weekday: 8/9` для `weekly`
- Вариант 2: `recurrence_type: "weekly"` + `anchor_weekday: 8/9` = `weekdays`/`weekends`
- Вариант 3: Удалить `weekdays`/`weekends` из `recurrence_type`, использовать только `anchor_weekday`

**Вывод:** Требование неоднозначно, нужна доработка.

---

### 11. Отсутствует описание `readable_config`

**Проблема:**
- Упоминается в терминах, но нет описания формата для разных типов задач

**Рекомендация:**
Добавить раздел с примерами `readable_config` для всех типов задач.

---

### 12. Неясность: что происходит с задачами при удалении группы

**Проблема:**
- Упоминается в веб-версии (строка 105), но нет описания в разделе БД/API

**Рекомендация:**
Добавить в раздел "БД" или "API": "При удалении группы все задачи группы получают `group_id = NULL`".

---

## 📋 Структурные улучшения

### 13. Добавить раздел "Текущее состояние vs Требования"

**Рекомендация:**
Добавить в начало документа:
```
### Статус документа

Этот документ описывает **требования к новой версии** системы. 

**Текущее состояние:**
- В БД используется `last_completed_at` (DateTime)
- В БД используется `next_due_date` (DateTime)
- В БД используется `is_active` (Boolean)

**Требования к новой версии:**
- Заменить `last_completed_at` на `completed` (Boolean)
- Удалить `next_due_date` (использовать только `reminder_time`)
- Добавить `anchor_weekday` (Integer NULL)

См. раздел "Миграции (план)" для деталей перехода.
```

---

### 14. Добавить примеры для всех типов задач

**Рекомендация:**
Расширить раздел "Примеры" (строка 423):
- Пример для `interval` задачи
- Пример для `recurring` с `anchor_weekday: 8` (будни)
- Пример для `monthly` с `anchor_weekday: NULL` (по числу)
- Пример для `monthly` с `anchor_weekday: 1` (понедельник)

---

### 15. Добавить описание валидации

**Рекомендация:**
Добавить раздел "Валидация данных":
- Ограничения на значения полей (`recurrence_interval >= 1`, `interval_days >= 1`)
- Проверки на уровне API (обязательные поля)
- Проверки на уровне БД (NOT NULL, FOREIGN KEY)

---

## ✅ Что хорошо сделано

1. **Детально описаны UI компоненты** — модальные окна, кнопки, свайпы
2. **Отлично описан механизм оповещений** — Android-клиент, откладывание, разрешения
3. **Чётко описаны форматы данных** — JSON примеры для API
4. **Есть план миграций** — понимание перехода от текущего состояния
5. **Хорошо описаны инварианты** — правила работы системы

---

## 🎯 Приоритеты доработки

### Критично (блокируют реализацию):
1. ✅ **Противоречие с `next_due_date`** (пункт 1) — решить, что использовать вместо
2. ✅ **Обязательность `reminder_time`** (пункт 2) — уточнить требование
3. ✅ **Расчёт следующего наступления** (пункт 4) — добавить детальное описание
4. ✅ **Временные зоны** (пункт 5) — описать обработку временных зон

### Важно (нужно для корректной работы):
5. ✅ **`completed` vs `last_completed_at`** (пункт 3) — описать миграцию данных
6. ✅ **`next_due_date` после миграции** (пункт 6) — описать, что использовать вместо
7. ✅ **Формат запроса при откладывании** (пункт 7) — уточнить формат

### Желательно (можно улучшить):
8. Остальные пункты (8-15)

---

## 📝 Итоговая оценка

**Общая оценка:** 7/10

**Сильные стороны:**
- Хорошо структурирован
- Детально описаны UI и механики оповещений
- Чёткие форматы данных

**Слабые стороны:**
- Противоречия в требованиях (пункты 1, 2)
- Неполные требования (пункты 4, 5, 6)
- Неясности в некоторых аспектах (пункты 10, 11, 12)

**Рекомендация:**
Документ можно использовать как основу для реализации, но перед началом разработки необходимо:
1. Разрешить противоречия (пункты 1, 2)
2. Дополнить критичные требования (пункты 4, 5)
3. Уточнить неясности (пункт 6)

После доработки документ станет полноценной спецификацией требований.
