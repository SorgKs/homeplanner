## Формат хранения и представления задач (БД, веб, Android)

Документ описывает единый контракт данных задач между БД, бэкендом (FastAPI), веб‑версией и Android‑клиентом, а также инварианты и примеры.

### Термины
- **reminder_time**: единственный источник истины по времени уведомления (планирование и показ). Следующее наступление рассчитывается от него и правил повторения. Хранится в локальном времени сервера (не UTC).
- **completed**: флаг «выполнено сегодня». На новый день сбрасывается автоматически в указанное время (настраивается в конфиге, по умолчанию 03:00).
- **readable_config**: человекочитаемое описание конфигурации задачи, формируется сервером.
- **Время сброса**: время начала нового дня, когда сервер сбрасывает флаг `completed = FALSE` для всех задач. Настраивается в конфигурации сервера, по умолчанию 03:00 (локальное время).

### База данных (логическая модель)
Таблица `tasks` (набор полей отражает модель SQLAlchemy и схемы Pydantic):
- id: INTEGER PK
- title: TEXT NOT NULL
- description: TEXT NULL
- task_type: TEXT NOT NULL — одно из: `one_time`, `recurring`, `interval`
- recurrence_type: TEXT NULL — одно из: `daily`, `weekdays`, `weekends`, `weekly`, `monthly`, `yearly`, `monthly_weekday`, `yearly_weekday`, `custom`, `interval` (для `recurring`)
- recurrence_interval: INTEGER NULL — шаг повторения (например, каждые N дней/недель/...)
- interval_days: INTEGER NULL — длительность окна для `interval`
- reminder_time: DATETIME NOT NULL — ISO 8601; всегда заполнено
  (единственный источник истины по дате и времени наступления)
- group_id: INTEGER NULL — ссылка на группу (FK)
- completed: BOOLEAN NOT NULL DEFAULT FALSE — выполнена ли задача сегодня
- enabled: BOOLEAN NOT NULL DEFAULT TRUE — включена ли задача (показывать/планировать)
- created_at / updated_at: DATETIME — служебные
- last_shown_at: DATETIME NULL — время последнего показа задачи пользователю

Таблица `task_user_assignment` (связь многие-ко-многим между задачами и пользователями):
- task_id: INTEGER FK → tasks.id
- user_id: INTEGER FK → users.id
- PRIMARY KEY (task_id, user_id)

**Назначение задач пользователям:**
- Задача может быть назначена нескольким пользователям (через таблицу `task_user_assignment`)
- При создании задачи можно указать список `assigned_user_ids` (массив ID пользователей)
- Если список пуст или не указан, задача считается общей (доступна всем пользователям)
- В API поле `assigned_user_ids` передаётся как массив целых чисел в JSON
- В модели SQLAlchemy реализовано через relationship с таблицей ассоциаций

Инварианты уровня БД/сервера:
- `reminder_time` всегда NOT NULL.
- `completed` отражает состояние выполнения только в пределах текущих суток; при наступлении нового дня (время сброса из конфига, по умолчанию 03:00) сервер сбрасывает `completed = FALSE`.
- `enabled = FALSE` исключает задачу из планирования и отображения по умолчанию.
- Расчёт следующего наступления выполняется от текущего `reminder_time` и правил повторения; для задач типа `interval` — от фактического времени выполнения (last_completed_at используется как служебное поле). Подробное описание алгоритма расчёта см. в разделе "Расчёт следующего наступления задачи".

### Бэкенд (FastAPI, Pydantic)
- Схемы: `TaskBase`, `TaskCreate`, `TaskUpdate`, `TaskResponse` (`backend/schemas/task.py`).
- Ключевая логика:
  - В `TaskBase` `reminder_time: datetime` (обязательное поле).
  - `TaskResponse.model_validate(...)` вычисляет `readable_config` через `TaskService._format_task_settings(...)`.
  - Бизнес‑логика: `backend/services/task_service.py`.
  - `_format_task_settings` для человекочитаемого описания. Показывает время из `reminder_time`.
  - При `create_task`/`update_task` сервер проверяет, что `reminder_time` установлен (обязательное поле).
  - **Планирование оповещений**:
    - При планировании оповещений сервер использует `reminder_time` для определения времени оповещения.
    - Откладывание оповещений обрабатывается локально в клиенте (Android-клиент), без передачи данных на сервер.

### API: представление задачи (JSON)
Ответы (`TaskResponse`) содержат:
```json
{
  "id": 1,
  "title": "Полить цветы",
  "description": "Южное окно",
  "task_type": "recurring",
  "recurrence_type": "weekly",
  "recurrence_interval": 1,
  "interval_days": null,
  "reminder_time": "2025-11-05T09:00:00",
  "group_id": 2,
  "completed": false,
  "active": true,
  "assigned_user_ids": [1, 2],
  "readable_config": "каждую неделю в 09:00"
}
```

**Поле `assigned_user_ids`:**
- Массив целых чисел (ID пользователей)
- Если массив пуст или отсутствует, задача доступна всем пользователям
- При фильтрации задач по пользователю учитываются только задачи, назначенные этому пользователю, или задачи без назначения (общие)

Создание (`POST /api/v1/tasks`), тело запроса (минимум):
```json
{
  "title": "Полить цветы",
  "task_type": "recurring",
  "recurrence_type": "weekly",
  "recurrence_interval": 1,
  "reminder_time": "2025-11-05T09:00:00",
  "assigned_user_ids": [1, 2]
}
```
Требование: поле `reminder_time` ОБЯЗАТЕЛЬНО при создании. Если не передано или пусто — ошибка 400 (validation error).

**Поле `assigned_user_ids` (необязательное):**
- Массив целых чисел (ID пользователей)
- Если не указано или пустой массив, задача считается общей (доступна всем)
- При создании задачи через Android приложение, если пользователи не указаны, автоматически назначается текущий пользователь

Обновление (`PATCH /api/v1/tasks/{id}`): частичное обновление. Передаются только изменяемые поля; остальные остаются без изменений. `reminder_time` обязателен только если изменяется. Никаких автоподстановок сервером.

Обновление совместимостью (`PUT /api/v1/tasks/{id}`): допускается полное обновление ресурса. Рекомендуется использовать `PATCH` в клиентах.

Эндпоинт "сегодня" (`GET /api/v1/tasks/today/ids`) возвращает **только массив идентификаторов** задач, актуальных на сегодня, отфильтрованных по текущему пользователю. 

**Требования:**
- Эндпоинт возвращает только список ID задач (не полные объекты).
- Cookie `hp.selectedUserId` с ID текущего пользователя обязателен.
- Возвращаются только задачи, назначенные указанному пользователю.
- Клиент использует полученные ID для фильтрации уже загруженных полных объектов задач.

Для показа времени использовать `reminder_time` из полных объектов задач. Поле `completed` в полных объектах указывает, выполнена ли задача сегодня.

### Веб‑версия
- Главный клиентский файл: `frontend/app.js`.
- **Виды (вкладки)**:
  - **"Сегодня"** (`today`): список задач, актуальных на сегодня (отфильтрованных бэкендом). Формат: слева время из `reminder_time`, центр — заголовок, справа — чекбокс.
  - **"Все задачи"** (`all`): полный список всех задач, сгруппированных по группам. Сначала активные задачи, затем в конце блок "Неактивные". Используется `task.readable_config` для отображения конфигурации.
  - **"История"** (`history`): история действий с задачами (создание, редактирование, выполнение и т.д.) с фильтрами по группам, задачам и датам.
  - **"Настройки"** (`settings`): настройки приложения.
- **Меню и навигация**:
  - Верхняя панель: кнопки переключения видов (Сегодня, Все задачи, История, Настройки).
  - Кнопка "Админ режим": переключает режим отображения (показывает дополнительные действия в истории).
  - Поиск и фильтры: доступны в верхней части для каждого вида.
- **Кнопки и действия**:
  - **"+" (добавить задачу)**: доступна только во вкладке "Все задачи", открывает модальное окно создания задачи.
  - **"✎" (редактировать)**: кнопка на карточке задачи, открывает модальное окно редактирования с предзаполненными полями из текущей задачи.
  - **"✕" (удалить)**: кнопка на карточке задачи, открывает диалог подтверждения удаления перед удалением задачи.
  - **Чекбокс**: на карточке задачи или в списке "Сегодня", отмечает задачу как выполненную/невыполненную. При изменении состояния автоматически отправляется запрос на сервер (`POST /api/v1/tasks/{id}/complete` или `POST /api/v1/tasks/{id}/uncomplete`).
  - **Фильтры**: кнопка "Фильтры" переключает состояние (Все / Только включенные / Только выполненные).
- **Подтверждения действий**:
  - **Удаление задачи**: при нажатии кнопки "✕" (удалить) на карточке задачи отображается стандартный диалог подтверждения браузера (`confirm()`), с текстом "Удалить задачу?". При подтверждении отправляется запрос `DELETE /api/v1/tasks/{id}`, задача удаляется из локального списка, отображается уведомление об успешном удалении. При отмене диалог закрывается, задача остаётся в списке.
  - **Удаление группы**: при удалении группы отображается диалог подтверждения с текстом "Удалить группу? Все задачи в группе останутся, но будут без группы.". При подтверждении отправляется запрос `DELETE /api/v1/groups/{id}`, группа удаляется из списка. При отмене диалог закрывается, группа остаётся.
  - **Удаление записи истории**: при удалении записи истории отображается диалог подтверждения с текстом "Вы уверены, что хотите удалить эту запись истории?". При подтверждении запись удаляется. При отмене диалог закрывается.
- **Модальные окна**:
  - **Создание/редактирование задачи**: содержит поля:
    - Название (обязательное, текст)
    - Описание (необязательное, текст)
    - Тип задачи (обязательное, выбор: `one_time`, `recurring`, `interval`)
    - Тип повторения (для `recurring`, выбор: `daily`, `weekdays`, `weekends`, `weekly`, `monthly`, `yearly`)
    - Интервал повторения (для `recurring`, число)
    - Дней интервала (для `interval`, число)
    - Дата/время напоминания (обязательное, ISO 8601, datetime-local) — одно поле, соответствует `reminder_time`
    - Группа (необязательное, выбор из списка)
    - Кнопки: "Сохранить", "Отмена"
  - **Заполнение при редактировании**: при открытии модального окна редактирования задачи все поля автоматически заполняются текущими параметрами задачи:
    - Данные берутся из объекта задачи в локальном списке `allTasks` (полученном из API или обновлённом через WebSocket)
    - Название: `task.title`
    - Описание: `task.description` (если есть, иначе пустая строка)
    - Тип задачи: `task.task_type`
    - Тип повторения: `task.recurrence_type` (если есть, иначе `null`)
    - Интервал повторения: `task.recurrence_interval` (если есть, иначе пустая строка)
    - Дней интервала: `task.interval_days` (если есть, иначе пустая строка)
    - Дата/время напоминания: `task.reminder_time` (если нет — текущая дата/время)
    - Группа: `task.group_id` (если есть, иначе пустое значение)
  - **Удаление задачи**: стандартный диалог подтверждения браузера (`confirm()`) с текстом "Удалить задачу?". При подтверждении отправляется запрос `DELETE /api/v1/tasks/{id}`, задача удаляется из локального списка, отображается уведомление об успешном удалении.
  - **Создание/редактирование группы**: содержит поля:
    - Название (обязательное, текст)
    - Описание (необязательное, текст)
    - Кнопки: "Сохранить", "Отмена"
- **Формат передачи данных на сервер**:
  - **Создание задачи** (`POST /api/v1/tasks`):
    ```json
    {
      "title": "Название задачи",
      "description": "Описание (необязательно)",
      "task_type": "one_time|recurring|interval",
      "recurrence_type": "daily|weekdays|weekends|weekly|monthly|yearly (для recurring)",
      "recurrence_interval": 1 (число, для recurring),
      "interval_days": 7 (число, для interval),
      "reminder_time": "2025-11-05T09:00:00" (ISO 8601, обязательное),
      "group_id": 1 (число или null, необязательное)
    }
    ```
    Все поля обязательны при создании, `reminder_time` обязателен всегда. При отсутствии обязательного поля — ошибка 400.
  - **Обновление задачи** (`PUT /api/v1/tasks/{id}`):
    ```json
    {
      "title": "Название задачи",
      "description": "Описание (необязательно)",
      "task_type": "one_time|recurring|interval",
      "recurrence_type": "daily|weekdays|weekends|weekly|monthly|yearly (для recurring)",
      "recurrence_interval": 1 (число, для recurring),
      "interval_days": 7 (число, для interval),
      "reminder_time": "2025-11-05T09:00:00" (ISO 8601, обязательное),
      "group_id": 1 (число или null, необязательное),
      "completed": false (boolean),
      "enabled": true (boolean)
    }
    ```
    Передаётся ПОЛНЫЙ объект задачи, все поля обязательны. Никаких автоподстановок или фоллбеков. При отсутствии любого обязательного поля — ошибка 400.
  - **Выполнение задачи** (`POST /api/v1/tasks/{id}/complete`): тело запроса пустое, ответ — обновлённый объект задачи.
  - **Отмена выполнения задачи** (`POST /api/v1/tasks/{id}/uncomplete`): тело запроса пустое, ответ — обновлённый объект задачи.
  - **Удаление задачи** (`DELETE /api/v1/tasks/{id}`): тело запроса пустое, ответ — статус 204 No Content.
  - **Создание группы** (`POST /api/v1/groups`):
    ```json
    {
      "name": "Название группы",
      "description": "Описание (необязательно, может быть null)"
    }
    ```
  - **Обновление группы** (`PUT /api/v1/groups/{id}`):
    ```json
    {
      "name": "Название группы",
      "description": "Описание (необязательно, может быть null)"
    }
    ```
  - **Удаление группы** (`DELETE /api/v1/groups/{id}`): тело запроса пустое, ответ — статус 204 No Content.
- **Отображение**:
  - Заголовок задачи формируется как: `<ИмяГруппы> <НазваниеЗадачи>` — сначала имя группы, затем через пробел название задачи. Если группа отсутствует, используется только название задачи без префикса.
  - В списке "Все задачи" используется `task.readable_config`. Сначала отображаются включенные задачи (сгруппированные по группам), затем в конце отдельным блоком "Отключенные" — задачи с `enabled = false` (также сгруппированные по группам).
  - В представлении "Сегодня" слева отображается время из `reminder_time`, центр — заголовок (в формате `<ИмяГруппы> <НазваниеЗадачи>`), справа — чекбокс.
- Формы:
  - Клиент обязан отправлять все обязательные поля; при создании и обновлении `reminder_time` обязателен. Сервер не выполняет автоподстановок, при отсутствии — ошибка 400.
  - Поля, скрытые по условиям UI, не должны иметь `required`.
- Веб‑сокеты: при событиях создаётся/обновляется локальный список и/или выполняется перезагрузка данных, чтобы задачи были консистентны.
 - Флаги: использовать `completed` вместо `last_completed_at`; добавлен флаг `enabled` для временной деактивации.
 - Привязка: поле `anchor_weekday` задаёт день недели (1=Пн … 7=Вс) для `weekly`; для `monthly`/`yearly` `NULL` означает «по числу», значение 1..7 — «по дню недели».

### Android‑клиент
- Модель: `android/app/src/main/java/com/homeplanner/model/Task.kt` (Kotlin data‑class), поля соответствуют JSON API.
- UI (Compose): `MainActivity.kt`.
- **Виды (вкладки)**:
  - **"Сегодня"** (`TODAY`): список задач, актуальных на сегодня (отфильтрованных бэкендом по текущему пользователю). Формат: слева время из `reminderTime`, центр — заголовок, справа — чекбокс и кнопка теста уведомления. Показываются только задачи, назначенные текущему пользователю (или общие задачи без назначения).
  - **"Все задачи"** (`ALL`): полный список всех задач для всех пользователей. По умолчанию показываются все задачи, но можно отфильтровать по конкретному пользователю. Сначала включенные задачи (`enabled = true`), затем в конце отдельным блоком "Отключенные" — задачи с `enabled = false`.
  - **"Настройки"** (`SETTINGS`): настройки приложения, версия.
  - **"WebSocket"** (`WEBSOCKET`): лог WebSocket-соединения для отладки.
- **Меню и навигация**:
  - Нижняя панель навигации (NavigationBar): кнопки переключения видов (Сегодня, Все задачи, Настройки, WebSocket).
  - Индикатор статуса подключения: в правом верхнем углу каждого вида показывается статус WebSocket-соединения (Подключено / Подключение... / Отключено) с цветным индикатором.
- **Кнопки и действия**:
  - **"+" (FloatingActionButton)**: доступна только во вкладке "Все задачи", открывает диалог создания задачи.
  - **Свайп по карточке задачи** (`SwipeToDismiss`):
    - **Свайп вправо** (`DismissDirection.EndToStart`): открывает диалог редактирования задачи с предзаполненными полями из текущей задачи.
    - **Свайп влево** (`DismissDirection.StartToEnd`): открывает диалог подтверждения удаления задачи.
    - Реализовано через `SwipeToDismiss` с `confirmStateChange` колбэком, который проверяет направление свайпа (`DismissValue.DismissedToStart` для влево, `DismissValue.DismissedToEnd` для вправо) и возвращает `false` для предотвращения автоматического удаления, вместо этого открывая соответствующий диалог.
    - При свайпе вправо (редактирование) карточка возвращается в исходное состояние после открытия диалога.
    - При свайпе влево (удаление) карточка возвращается в исходное состояние, и показывается диалог подтверждения.
    - Работает для всех задач в представлении "Все задачи" (как активных, так и неактивных).
  - **Чекбокс**: на карточке задачи отмечает задачу как выполненную/невыполненную. При изменении состояния автоматически отправляется запрос на сервер (`POST /api/v1/tasks/{id}/complete` или `POST /api/v1/tasks/{id}/uncomplete`).
  - **Кнопка уведомления (🔔)**: во вкладке "Сегодня" на каждой задаче, запускает тестовое уведомление для отладки.
- **Подтверждения действий**:
  - **Удаление задачи** (`AlertDialog`):
    - Отображается при свайпе влево по карточке задачи.
    - Содержит заголовок "Удалить задачу?", текст "Действие нельзя отменить.", кнопки "Удалить" (confirmButton) и "Отмена" (dismissButton).
    - При нажатии "Удалить" отправляется запрос `DELETE /api/v1/tasks/{id}`, задача удаляется из локального списка, диалог закрывается.
    - При нажатии "Отмена" диалог закрывается, задача остаётся в списке.
- **Модальные окна (диалоги)**:
  - **Создание/редактирование задачи** (AlertDialog): содержит поля (в порядке отображения):
    - Название (обязательное, OutlinedTextField)
    - Группа (необязательное, ExposedDropdownMenuBox: выбор из списка групп) — расположено сразу после названия
    - Тип задачи (обязательное, ExposedDropdownMenuBox: `one_time` → "Разовая", `recurring` → "Расписание", `interval` → "Интервальная", все переведены на русский)
    - Тип повторения (для `recurring`, ExposedDropdownMenuBox: `daily` → "Ежедневно", `weekly` → "Еженедельно", `monthly` → "Ежемесячно", `yearly` → "Ежегодно", все переведены на русский, по умолчанию "Ежедневно", скрыто для не-recurring задач)
    - Интервал (для `recurring`, OutlinedTextField, число) — переименовано с "Интервал повторения", скрыто для разовых задач
    - Интервал (для `interval`, OutlinedTextField, число) — переименовано с "Дней интервала", скрыто для разовых задач
    - Дата/время напоминания (обязательное, OutlinedTextField + TextButton "Выбрать" для вызова DatePicker/TimePicker, ISO 8601) — одно поле, соответствует `reminderTime`
    - Назначено пользователям (необязательное, ExposedDropdownMenuBox с множественным выбором: список активных пользователей, по умолчанию выбран текущий пользователь)
    - Описание (необязательное, OutlinedTextField) — расположено в конце формы
    - Кнопки: "Сохранить" (confirmButton), "Отмена" (dismissButton)
  - **Заполнение при редактировании**: при открытии диалога редактирования задачи все поля автоматически заполняются текущими параметрами задачи:
    - Данные берутся из объекта задачи в локаль списке `tasks` (полученном из API или обновлённом через WebSocket)
    - Название: `task.title`
    - Группа: `task.groupId` (если есть, иначе `null`)
    - Тип задачи: `task.taskType`
    - Тип повторения: `task.recurrenceType` (если есть и тип `recurring`, иначе по умолчанию "daily")
    - Интервал повторения: `task.recurrenceInterval` (если есть, иначе пустая строка)
    - Дней интервала: `task.intervalDays` (если есть, иначе пустая строка)
    - Дата/время напоминания: `task.reminderTime` (если нет — текущая дата/время)
    - Назначено пользователям: `task.assignedUserIds` (если есть, иначе текущий пользователь по умолчанию)
    - Описание: `task.description` (если есть и не равно "null", иначе пустая строка)
  - **Удаление задачи** (AlertDialog): диалог подтверждения с текстом "Удалить задачу?" и кнопками "Удалить" (confirmButton), "Отмена" (dismissButton).
- **Формат передачи данных на сервер**:
  - **Создание задачи** (`POST /api/v1/tasks`):
    ```json
    {
      "title": "Название задачи",
      "description": "Описание (необязательно, может быть null)",
      "task_type": "one_time|recurring|interval",
      "recurrence_type": "daily|weekly|monthly|yearly (для recurring, по умолчанию daily)",
      "recurrence_interval": 1 (число или null, для recurring),
      "interval_days": 7 (число или null, для interval),
      "reminder_time": "2025-11-05T09:00:00" (ISO 8601, обязательное),
      "group_id": 1 (число или null, необязательное),
      "assigned_user_ids": [1, 2] (массив чисел, необязательное, по умолчанию текущий пользователь)
    }
    ```
    Все поля обязательны при создании, `reminder_time` обязателен всегда. При отсутствии обязательного поля — ошибка 400.
    Поле `assigned_user_ids` необязательно: если не указано или пустой массив, автоматически назначается текущий пользователь при создании через Android приложение.
    Для задач типа `recurring` поле `recurrence_type` по умолчанию устанавливается в `daily`, если не указано.
  - **Обновление задачи** (`PUT /api/v1/tasks/{id}`):
    ```json
    {
      "title": "Название задачи",
      "description": "Описание (необязательно, может быть null)",
      "task_type": "one_time|recurring|interval",
      "recurrence_type": "daily|weekly|monthly|yearly (для recurring, null для не-recurring)",
      "recurrence_interval": 1 (число или null, для recurring),
      "interval_days": 7 (число или null, для interval),
      "reminder_time": "2025-11-05T09:00:00" (ISO 8601, обязательное),
      "group_id": 1 (число или null, необязательное),
      "assigned_user_ids": [1, 2] (массив чисел, необязательное, пустой массив для общих задач),
      "completed": false (boolean, необязательное при обновлении),
      "enabled": true (boolean, необязательное при обновлении)
    }
    ```
    Передаётся ПОЛНЫЙ объект задачи, все поля обязательны. Никаких автоподстановок или фоллбеков. При отсутствии любого обязательного поля — ошибка 400.
    Для задач типа `one_time` или `interval` поле `recurrence_type` должно быть `null`.
    Поле `assigned_user_ids` необязательно: если не указано или пустой массив, задача считается общей (доступна всем пользователям).
  - **Выполнение задачи** (`POST /api/v1/tasks/{id}/complete`): тело запроса пустое, ответ — обновлённый объект задачи.
  - **Отмена выполнения задачи** (`POST /api/v1/tasks/{id}/uncomplete`): тело запроса пустое, ответ — обновлённый объект задачи.
  - **Удаление задачи** (`DELETE /api/v1/tasks/{id}`): тело запроса пустое, ответ — статус 204 No Content.
- **Отображение**:
  - Заголовок задачи формируется как: `<ИмяГруппы> <НазваниеЗадачи>` — сначала имя группы, затем через пробел название задачи. Если группа отсутствует, используется только название задачи.
  - В списке "Все задачи" сначала отображаются включенные задачи (`enabled = true`), затем в конце отдельным блоком "Отключенные" — задачи с `enabled = false`.
  - В списке и в окне редактирования время берётся из `reminderTime`.
  - При сохранении, если `reminderTime` не задан, клиент/сервер вычисляют его согласно правилам (сервер гарантирует NOT NULL).
- **Платёжеспособность API**:
  - Обновление задач через `PUT` (не `PATCH`). См. `android/app/src/main/java/com/homeplanner/api/TasksApi.kt`.
- **Работа с оповещениями (notifications/alarms)**:
  - **Важно: оповещения работают даже когда приложение закрыто**:
    - `AlarmManager` работает на уровне системы Android, независимо от состояния приложения.
    - `ReminderReceiver` зарегистрирован в манифесте (`AndroidManifest.xml`) как статический BroadcastReceiver, поэтому получает события даже когда приложение не запущено.
    - Используется `AlarmManager.setExactAndAllowWhileIdle()` (Android M+) или `setExact()` (старые версии) с флагом `RTC_WAKEUP`, который разбудит устройство из режима сна, если это необходимо.
    - При срабатывании будильника система автоматически запускает `ReminderReceiver`, даже если приложение было закрыто пользователем или системой.
  - **Планирование напоминаний** (`ReminderScheduler`):
    - Используется Android `AlarmManager` для планирования точных будильников (exact alarms).
    - Для каждой задачи с `enabled = true` создаётся точный будильник.
    - Время будильника берётся из `task.reminderTime` (ISO 8601, парсится в `LocalDateTime`).
    - Отложенные оповещения обрабатываются локально в клиенте через `AlarmManager`, без синхронизации с сервером.
    - Будильник создаётся только если вычисленное время в будущем.
    - При обновлении задач (создание, изменение, удаление) все будильники перепланируются: сначала отменяются все существующие (`cancelAll`), затем создаются новые для актуальных задач (`scheduleForTasks`).
    - Требуется разрешение `SCHEDULE_EXACT_ALARM` (Android 12+), которое запрашивается автоматически при первом запуске приложения через открытие настроек системы. Без этого разрешения на Android 12+ оповещения не будут работать.
    - Методы планирования:
      - Android M+ (API 23+): `alarmManager.setExactAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, whenMs, pi)` — работает даже в режиме Doze и при выключенном экране.
      - Старые версии: `alarmManager.setExact(AlarmManager.RTC_WAKEUP, whenMs, pi)` — работает при закрытом приложении.
  - **Получение оповещения** (`ReminderReceiver`):
    - `ReminderReceiver` — BroadcastReceiver, зарегистрирован в манифесте как статический receiver (`<receiver android:name=".ReminderReceiver" ... />`).
    - Получает события от `AlarmManager` при срабатывании будильника, даже когда приложение закрыто.
    - При получении оповещения:
      1. Извлекает данные задачи из Intent (title, message, taskId).
      2. Запускает полноэкранную активность `ReminderActivity` для отображения будильника поверх заблокированного экрана.
      3. Создаёт стандартное уведомление через `NotificationHelper` для панели уведомлений.
  - **Полноэкранное оповещение** (`ReminderActivity`):
    - Отображается поверх заблокированного экрана (флаги: `FLAG_SHOW_WHEN_LOCKED`, `FLAG_TURN_SCREEN_ON`, `FLAG_KEEP_SCREEN_ON`, `FLAG_DISMISS_KEYGUARD`, `FLAG_FULLSCREEN`).
    - Воспроизводит звук будильника:
      - Использует `MediaPlayer` для воспроизведения звука с зацикливанием (looping).
      - Альтернативно может использовать `RingtoneManager` для системного звука будильника.
      - Настроен на поток `STREAM_ALARM` с уровнем громкости `RINGER_MODE_NORMAL`.
    - Удерживает `WakeLock` для предотвращения перехода устройства в спящий режим во время оповещения.
    - Отображает информацию о задаче: заголовок (с группой, если есть), описание (если есть).
    - Кнопки действий:
      - **"Отложить на 10 минут"**: откладывает оповещение на 10 минут.
      - **"Отложить на час"**: откладывает оповещение на 1 час.
      - **"Отложить на день"**: откладывает оповещение на 1 день.
      - **"Выполнено"**: отмечает задачу как выполненную через API (`POST /api/v1/tasks/{id}/complete`), останавливает звук, освобождает WakeLock и закрывает активность.
    - **Механизм откладывания оповещения**:
      - При нажатии любой из кнопок откладывания (`onSnooze`):
        1. Останавливается звук будильника (`stopAlarmSound()`).
        2. Освобождается WakeLock (`releaseWakeLock()`).
        3. Вызывается метод `scheduleSnooze(title, message, taskId, delayMinutes)`, где:
           - `title` — заголовок задачи (из Intent).
           - `message` — сообщение задачи (из Intent).
           - `taskId` — идентификатор задачи (из Intent, если есть, иначе -1).
           - `delayMinutes` — задержка в минутах (10 для "10 минут", 60 для "час", 1440 для "день").
        4. Внутри `scheduleSnooze`:
           - Создаётся новый Intent для `ReminderReceiver` с теми же данными задачи (title, message, taskId).
           - Создаётся `PendingIntent` с уникальным идентификатором (используется `taskId`, если он есть, иначе `System.currentTimeMillis().toInt()`).
           - Вычисляется время срабатывания нового оповещения: `System.currentTimeMillis() + delayMinutes * 60_000L`.
           - Планируется новое оповещение через `AlarmManager.setExactAndAllowWhileIdle()` (Android M+) или `setExact()` (старые версии) с флагом `RTC_WAKEUP`.
           - Новое оповещение будет срабатывать точно в указанное время, даже если устройство находится в режиме сна или приложение закрыто.
           - **Важно**: откладывание оповещения выполняется только локально в клиенте, никакие данные на сервер не передаются.
           - Поле `reminder_time` задачи на сервере НЕ изменяется — оно остаётся исходным временем оповещения.
        5. Закрывается текущая активность (`finish()`).
      - После откладывания:
        - Окно оповещения закрывается, звук останавливается, устройство может вернуться в режим сна.
        - В указанное время (через 10 минут, час или день) сработает новое оповещение через `ReminderReceiver`, которое откроет `ReminderActivity` заново.
        - Отложенное оповещение существует только локально в клиенте, никакие данные на сервер не передаются.
        - При следующей загрузке задач (`loadTasks`) или через WebSocket событие `task_update` оповещения перепланируются согласно серверному `reminder_time` (если отложенное оповещение ещё не сработало, оно будет перезаписано обычным оповещением из `reminder_time`).
      - Особенности:
        - Откладывание не изменяет статус задачи (не отмечает как выполненную).
        - Откладывание не влияет на поле `completed` задачи.
        - Откладывание работает даже если приложение закрыто (опирается на `AlarmManager`).
        - Если пользователь отложил оповещение, но затем отметил задачу как выполненную через основной интерфейс приложения, отложенное оповещение всё равно сработает (это можно улучшить в будущем, добавив проверку статуса задачи в `ReminderReceiver`).
        - Если пользователь отложил оповещение несколько раз подряд, каждое новое откладывание создаёт новое оповещение, но старое не отменяется автоматически (можно улучшить, сохраняя идентификатор последнего отложенного оповещения и отменяя его при новом откладывании).
    - При закрытии активности (`onDestroy`) автоматически останавливается звук и освобождается WakeLock.
  - **Уведомления** (`NotificationHelper`):
    - Создаёт стандартные уведомления для панели уведомлений Android.
    - Требуется разрешение `POST_NOTIFICATIONS` (Android 13+), запрашивается автоматически при первом запуске приложения.
    - Уведомление содержит:
      - Заголовок: название задачи (с группой, если есть).
      - Текст: описание задачи или сообщение о напоминании.
      - При нажатии открывает приложение (запускает `MainActivity`).
  - **Запрос разрешений**:
    - Все необходимые разрешения запрашиваются автоматически при первом запуске приложения в методе `MainActivity.ensurePermissions()`:
      - `POST_NOTIFICATIONS` (Android 13+) — запрашивается через Activity Result API (`requestNotificationPermissionLauncher`).
      - `SCHEDULE_EXACT_ALARM` (Android 12+) — открывается окно настроек системы для предоставления разрешения через Intent (`Settings.ACTION_REQUEST_SCHEDULE_EXACT_ALARM`).
    - Используется современный Activity Result API для обработки результатов запросов разрешений.
    - При отсутствии разрешений запросы выполняются автоматически при каждом запуске приложения до тех пор, пока разрешения не будут предоставлены.
  - **Тестовое оповещение**:
    - Кнопка теста уведомления (🔔) в представлении "Сегодня" запускает `ReminderReceiver.onReceive` напрямую с данными задачи.
    - Используется для отладки и проверки работы оповещений без ожидания времени срабатывания.
  - **Перепланирование**:
    - При загрузке задач из API (`loadTasks`) автоматически перепланируются все напоминания.
    - При обновлении задач через WebSocket (события `created`, `updated`, `deleted`) также выполняется перепланирование.
    - При смене вкладки "Сегодня" (`ViewTab.TODAY`) и перезагрузке задач выполняется перепланирование.
  - **Напоминания**: `ReminderScheduler` использует только `reminderTime` для планирования будильника.
 - **Флаги**: использовать `completed` (сбрасывается сервером на новый день) и `enabled` (отключает планирование/показ задачи при `false`).
 - **Привязка**: клиент получает/передаёт `anchor_weekday` по тем же правилам (1=Пн … 7=Вс; `NULL` — привязка к числу для `monthly`/`yearly`).

### Веб‑сокет события
Сервер рассылает сообщения типа:
```json
{
  "type": "task_update",
  "action": "created | updated | deleted | completed | uncompleted | shown",
  "task_id": 123,
  "task": { /* объект задачи как в ответе API, может отсутствовать */ }
}
```
Клиенты обязаны:
- При `created/updated` — обновить/вставить задачу и перепланировать локальные напоминания.
- Для вкладки "Сегодня" — предпочтительно перезагружать список с бэкенда (серверная фильтрация истиннее).
 - События `completed/uncompleted` отражают смену флага `completed` в текущие сутки.

Подключение и переподключение:
- URL: `ws://<HOST>:8000/ws`.
- При ошибке/закрытии соединения клиент выполняет переподключение с экспоненциальным бэкоффом и джиттером (базовый интервал 2–10 сек, максимум 60 сек).
- Сообщения применяются в порядке получения; при сомнениях предпочтительно перезагружать данные с бэкенда для консистентности.

### Время и сброс дня

**Локальное время:**
- Вся система работает с локальным временем сервера (не используется UTC).
- Все даты и время в базе данных хранятся в локальном времени сервера (без информации о временной зоне).
- Клиенты должны отправлять даты в формате ISO 8601 без информации о временной зоне (локальное время).
- Проект не использует таймзоны — никаких пересчётов и конвертаций между таймзонами.

**DST (переход на летнее/зимнее время):**
- При планировании сохраняется «видимое» локальное время (часы/минуты) пользователя; при сдвиге DST допускается фактическое смещение ±1 час.
- Для «пропавших» часов (spring forward) событие переносится на ближайшее возможное системное время с сохранением минут.
- Для «дублирующихся» часов (fall back) выбирается первое наступление.

**Начало нового дня и сброс `completed`:**
- Время начала нового дня (сброс флага `completed = FALSE`) настраивается в конфигурации сервера.
- По умолчанию: 03:00 (локальное время сервера).
- Сервер выполняет сброс `completed = FALSE` для всех задач в указанное время (например, в 03:00:01).
- После сброса генерируются WebSocket события `uncompleted` для всех задач, которые были выполнены вчера.
- Периодическая задача (cron/scheduler) запускается в указанное время каждый день для выполнения сброса.

**Пример:**
- Если время сброса = 03:00, то новый день начинается в 03:00:01.
- Задача, выполненная 2025-11-05 14:30, будет иметь `completed = TRUE` до 2025-11-06 03:00:01.
- После 2025-11-06 03:00:01 флаг `completed` автоматически сбросится в `FALSE`.

### Инварианты и правила
- Всегда показываем и планируем по `reminder_time` (NOT NULL).
- `completed` означает выполнено сегодня; при начале нового дня (время сброса из конфига, по умолчанию 03:00) сервер сбрасывает `completed = FALSE`.
- `active = FALSE` исключает из показа/планирования.
- Формат дат: ISO 8601 локальный `YYYY-MM-DDTHH:MM:SS[.ffffff]` (микросекунды возможны, без информации о временной зоне).
- Все операции с датами и временем выполняются в локальном времени сервера (не UTC).
- Совместимость: для старых записей с `reminder_time = NULL` при миграции будет заполнено валидным временем.

### Обработка ошибок

#### Бэкенд (FastAPI)
**HTTP статус коды:**
- **200 OK**: успешный запрос (GET, PUT для существующих ресурсов).
- **201 Created**: успешное создание ресурса (POST).
- **204 No Content**: успешное удаление (DELETE).
- **400 Bad Request**: некорректный запрос (невалидные данные, отсутствие обязательных полей, общие ошибки валидации).
- **404 Not Found**: ресурс не найден (задача/группа с указанным ID не существует).
- **422 Unprocessable Entity**: ошибка валидации данных Pydantic или ошибка бизнес-логики (`ValueError` из сервиса).
- **500 Internal Server Error**: внутренняя ошибка сервера (необработанные исключения).

**Валидация и обработка ошибок:**
- **Pydantic валидация**: автоматически проверяет все входящие данные по схемам (`TaskCreate`, `TaskUpdate`, `TaskBase`). При ошибке валидации FastAPI автоматически возвращает 422 с детальным описанием ошибок в формате:
  ```json
  {
    "detail": [
      {
        "loc": ["body", "reminder_time"],
        "msg": "field required",
        "type": "value_error.missing"
      }
    ]
  }
  ```
- **Обязательные поля**: если при создании/обновлении задачи отсутствует обязательное поле (например, `reminder_time`), Pydantic вернёт 422 с описанием отсутствующих полей.
- **Бизнес-логика**: ошибки валидации бизнес-логики в `TaskService` (например, невалидная комбинация `task_type` и `recurrence_type`) обрабатываются через `ValueError`, который преобразуется в 422 в роутере.
- **Несуществующие ресурсы**: при попытке получить/обновить/удалить задачу или группу с несуществующим ID возвращается 404 с сообщением `"Task with id {task_id} not found"` или `"Group with id {group_id} not found"`.
- **Обработка исключений**: неожиданные исключения в роутерах обрабатываются через `try/except`, преобразуются в 400 для предотвращения 500 ошибок и проблем с CORS.
- **Логирование**: все ошибки логируются через `logging` модуль Python (`logger.error`, `logger.exception`).

**Примеры ошибок:**
- **Создание задачи без `reminder_time`**: 422, `{"detail": [{"loc": ["body", "reminder_time"], "msg": "field required", ...}]}`
- **Обновление несуществующей задачи**: 404, `{"detail": "Task with id 999 not found"}`
- **Невалидный `task_type`**: 422, `{"detail": "Invalid task_type value"}`
- **Ошибка подключения к БД**: 500 (или 400, если обработано в try/except)

#### Веб‑версия (JavaScript)
**Обработка HTTP ошибок:**
- **Проверка статуса**: после каждого HTTP запроса проверяется `response.ok`. Если `response.ok === false`, выбрасывается исключение с описанием ошибки.
- **Парсинг сообщений об ошибках**: при ошибке 400/422 клиент пытается распарсить JSON ответ с полем `detail`:
  ```javascript
  try {
    const errorData = await response.json();
    if (Array.isArray(errorData.detail)) {
      // Pydantic validation errors
      errorMessage = errorData.detail.map(e => `${e.loc.join('.')}: ${e.msg}`).join(', ');
    } else if (errorData.detail) {
      errorMessage = errorData.detail;
    }
  } catch (e) {
    console.error('Failed to parse error response:', e);
  }
  ```
- **Отображение ошибок пользователю**: ошибки отображаются через `showToast(errorMessage, 'error')` с красным фоном и текстом ошибки.
- **Обработка сетевых ошибок**: ошибки сети (например, недоступность сервера) обрабатываются через `try/catch` и отображаются пользователю с сообщением "Ошибка загрузки данных. Убедитесь, что backend запущен."
- **Логирование**: все ошибки логируются в консоль через `console.error()` с описанием операции и деталями ошибки.

**Обработка WebSocket ошибок:**
- **Ошибка подключения**: при ошибке подключения (`ws.onerror`) ошибка логируется в консоль, соединение закрывается, через 2 секунды выполняется повторная попытка подключения.
- **Ошибка парсинга сообщения**: при ошибке парсинга JSON сообщения (`JSON.parse`) ошибка логируется, сообщение игнорируется, соединение остаётся активным.
- **Закрытие соединения**: при закрытии соединения (`ws.onclose`) логируется сообщение, через 2 секунды выполняется повторная попытка подключения.
- **Автоматическое переподключение**: при любой ошибке WebSocket выполняется автоматическое переподключение через 2 секунды с бесконечными повторными попытками.

**Примеры обработки:**
- **Ошибка создания задачи**: `showToast("reminder_time: field required", 'error')`
- **Ошибка обновления задачи**: `showToast("Task with id 999 not found", 'error')`
- **Недоступность сервера**: `showToast("Ошибка загрузки данных. Убедитесь, что backend запущен.", 'error')`
- **Ошибка WebSocket**: логируется в консоль, выполняется переподключение

#### Android‑клиент (Kotlin)
**Обработка HTTP ошибок:**
- **Проверка статуса**: после каждого HTTP запроса проверяется `response.isSuccessful`. Если `response.isSuccessful === false`, выбрасывается `IllegalStateException("HTTP ${response.code}")` или `IllegalStateException("HTTP ${response.code}: $errorBody")` с телом ответа.
- **Парсинг сообщений об ошибках**: при ошибке клиент пытается прочитать тело ответа (`response.body?.string()`) и включить его в сообщение об ошибке для логирования.
- **Обработка исключений**: все HTTP запросы обёрнуты в `try/catch` для обработки сетевых ошибок (например, `UnknownHostException`, `ConnectException`). Ошибки логируются через `Log.e()` и пробрасываются дальше.
- **Логирование**: все ошибки логируются через `Log.d()` (успешные запросы), `Log.e()` (ошибки) с указанием URL, кода ответа, тела запроса/ответа.

**Обработка ошибок разрешений:**
- **Отсутствие разрешений**: при отсутствии необходимых разрешений (`POST_NOTIFICATIONS`, `SCHEDULE_EXACT_ALARM`) запросы разрешений выполняются автоматически при каждом запуске приложения до тех пор, пока разрешения не будут предоставлены.
- **Ошибка запроса разрешений**: ошибки запроса разрешений обрабатываются через Activity Result API, логируются, но не блокируют работу приложения (оповещения просто не будут работать без разрешений).

**Обработка ошибок планирования оповещений:**
- **Ошибка планирования**: при ошибке планирования оповещения через `AlarmManager` (например, отсутствие разрешения `SCHEDULE_EXACT_ALARM`) ошибка логируется через `Log.e()`, но не блокирует работу приложения (оповещения просто не будут срабатывать).
- **Ошибка парсинга времени**: при ошибке парсинга `reminder_time` (невалидный формат ISO 8601) ошибка логируется, задача пропускается при планировании оповещений.

**Обработка ошибок WebSocket:**
- **Ошибка подключения**: при ошибке подключения WebSocket ошибка логируется, выполняется повторная попытка подключения.
- **Ошибка парсинга сообщения**: при ошибке парсинга JSON сообщения ошибка логируется, сообщение игнорируется, соединение остаётся активным.
- **Закрытие соединения**: при закрытии соединения логируется сообщение, выполняется повторная попытка подключения.

**Примеры обработки:**
- **Ошибка создания задачи**: `Log.e("TasksApi", "HTTP error: code=422, body={\"detail\":[{\"loc\":[\"body\",\"reminder_time\"],...}]}")`, исключение пробрасывается в UI
- **Недоступность сервера**: `Log.e("TasksApi", "Error creating task", e)` (например, `UnknownHostException`), исключение пробрасывается в UI
- **Ошибка планирования оповещения**: `Log.e("ReminderScheduler", "Failed to schedule alarm for task ${task.id}", e)`, задача пропускается
- **Отсутствие разрешения**: разрешение запрашивается автоматически, оповещения не работают до предоставления разрешения

### Расчёт следующего наступления задачи

После выполнения задачи (`completed = TRUE`) сервер рассчитывает следующее `reminder_time`:

1. **`one_time` задачи:**
   - После выполнения задача не повторяется
   - `reminder_time` не изменяется (остаётся прежним)
   - Задача остаётся в базе данных, но больше не будет показываться в списке "Сегодня" (если не выполнена сегодня)
   - При сбросе `completed = FALSE` на новый день задача может снова появиться в списке "Сегодня", если `reminder_time` соответствует сегодняшнему дню

2. **`recurring` задачи:**
   - Расчёт от текущего `reminder_time` + правила повторения.
   - **`daily`**: `reminder_time + N дней`.
   - **`weekly`**:
     - При `anchor_weekday` 1..7: ближайший следующий указанный день недели, учитывая `recurrence_interval` (недели).
     - При `anchor_weekday = 8` (будни): ближайший следующий день Пн–Пт.
     - При `anchor_weekday = 9` (выходные): ближайший следующий день Сб–Вс.
   - **`monthly`**:
     - Если `anchor_weekday IS NULL`: повтор «по числу» исходной даты. Если числа нет в месяце (например, 31‑е), правило: взять последний день месяца.
     - Если `anchor_weekday = 1..7`: выбрать ближайший следующий указанный день недели в целевом месяце (например, первый понедельник). Если требуется «N‑й понедельник» — специфицировать отдельно, по умолчанию «первый подходящий».
     - Если `anchor_weekday = 8/9`: ближайший следующий будний/выходной в целевом месяце.
   - **`yearly`**:
     - Если `anchor_weekday IS NULL`: повтор по календарной дате (MM‑DD). Для 29‑фев — правило: 28‑фев в невисокосные годы.
     - Если `anchor_weekday = 1..7/8/9`: аналогично monthly, но в разрезе следующего года (например, «первый понедельник ноября»).
   - **`weekdays`/`weekends`**: эквивалент `weekly` с `anchor_weekday=8/9`.

3. **`interval` задачи:**
   - Расчёт от фактического времени выполнения (`last_completed_at` в момент успешного `complete`) + `interval_days`.
   - Если задача выполнена сегодня, новый `reminder_time` = сегодня + `interval_days` дней в то же время (время берётся из текущего `reminder_time`)
   - Пример: если задача выполнена 2025-11-05 14:30, и `interval_days = 7`, то новый `reminder_time = 2025-11-12 14:30`
   - Если задача выполнена не сегодня, расчёт выполняется от даты выполнения (не от текущего `reminder_time`)

**Важные детали:**
- Расчёт выполняется автоматически при установке `completed = TRUE` (при выполнении задачи через `POST /api/v1/tasks/{id}/complete`)
- Время (часы и минуты) из текущего `reminder_time` сохраняется для всех типов задач, кроме случаев, когда `anchor_weekday` требует другого времени
- При расчёте используется локальное время сервера (не UTC)
- Если расчётное время попадает в прошлое (например, из‑за выполнения задачи не в срок), используется это время — задача будет показана как просроченная при следующей проверке
 - При переходах DST сохраняется «видимое» локальное время пользователя: в день сдвига разрешён ±1 час фактического системного времени.

### Будни/выходные и привязка к дням
- Для `weekly` выбор осуществляется через `recurrence_type`: `weekdays` (будни), `weekends` (выходные), или конкретный день (дополнительная логика не реализована).
- Для `daily` нет специальной привязки.
- Для `monthly`/`yearly` дополнительная логика привязки к дням недели не реализована.

### Миграции (план)
1) `reminder_time` -> NOT NULL:
   - Обновить все `NULL` валидными значениями (для старых записей использовать текущую дату/время или значение из существующих данных);
   - Поставить `NOT NULL` на колонку.
2) Удалить `next_due_date`:
   - Перенести логику расчёта следующего наступления на `reminder_time` + правила повторения;
   - Обновить индексы/запросы для выборок «на сегодня/просрочено» по `reminder_time`;
   - Удалить колонку `next_due_date` из таблицы `tasks`.
3) Заменить `last_completed_at` на `completed BOOLEAN NOT NULL DEFAULT FALSE` и репрофилировать `last_completed_at` как служебное поле:
   - **Добавить колонку `completed`**:
     - Тип: `BOOLEAN NOT NULL DEFAULT FALSE`
     - Создать колонку с временным значением по умолчанию `FALSE`
   - **Миграция данных из `last_completed_at` в `completed`**:
     - Для каждой записи в таблице `tasks`:
       - Если `last_completed_at IS NOT NULL`:
         - Получить дату из `last_completed_at` (без времени): `DATE(last_completed_at)`
         - Получить сегодняшнюю дату: `DATE(NOW())`
         - Если `DATE(last_completed_at) = DATE(NOW())`:
           - Установить `completed = TRUE` (задача выполнена сегодня)
         - Если `DATE(last_completed_at) < DATE(NOW())`:
           - Установить `completed = FALSE` (задача выполнена вчера или раньше, значит сегодня не выполнена)
       - Если `last_completed_at IS NULL`:
         - Установить `completed = FALSE` (задача никогда не выполнялась)
     - SQL-пример миграции:
       ```sql
       ALTER TABLE tasks ADD COLUMN completed BOOLEAN NOT NULL DEFAULT FALSE;
       UPDATE tasks 
       SET completed = CASE 
           WHEN last_completed_at IS NOT NULL 
                AND DATE(last_completed_at) = DATE(NOW()) 
           THEN TRUE 
           ELSE FALSE 
       END;
       ```
   - **Обновить код сервера**:
     - Заменить все проверки `task.last_completed_at is not None` на `task.completed == True`
     - Заменить все проверки `task.last_completed_at is None` на `task.completed == False`
     - При выполнении задачи (`POST /api/v1/tasks/{id}/complete`):
       - Устанавливать `completed = TRUE` и писать `last_completed_at = datetime.now()`
     - При отмене выполнения (`POST /api/v1/tasks/{id}/uncomplete`):
       - Устанавливать `completed = FALSE` (значение `last_completed_at` не обнуляется, остаётся как штамп последнего выполнения)
     - Добавить периодическую задачу (cron/scheduler) для сброса `completed = FALSE` на новый день:
       - Запускать в указанное время начала нового дня (настраивается в конфигурации, по умолчанию 03:00:01)
       - Обновлять все задачи с `completed = TRUE` на `completed = FALSE`
       - Генерировать WebSocket события `uncompleted` для всех задач, которые были выполнены вчера
       - Время сброса настраивается в конфигурации сервера (переменная `RESET_TIME` или аналогичная), по умолчанию 03:00
     - Обновить логику фильтрации задач:
       - В `get_tasks_for_today()`: проверять `task.completed` вместо `task.last_completed_at`
       - В `get_all_tasks()`: убрать логику обновления `last_completed_at`
   - **Обновить схемы Pydantic**:
     - В `TaskBase`: заменить `last_completed_at: datetime | None` на `completed: bool = Field(False, ...)`
     - В `TaskUpdate`: заменить `last_completed_at: datetime | None` на `completed: bool | None = None`
     - В `TaskResponse`: включить `completed: bool` в ответ
     - Исключить `last_completed_at` из публичных схем ответов (не возвращать в API), использовать только внутри сервиса расчёта для `interval`
   - **Обновить API эндпоинты**:
     - `POST /api/v1/tasks/{id}/complete`: обновлять `completed = TRUE`
     - `POST /api/v1/tasks/{id}/uncomplete`: обновлять `completed = FALSE`
     - Убрать обработку `last_completed_at` из всех эндпоинтов
   - **Обновить клиенты**:
     - Веб-версия: заменить проверки `task.last_completed_at` на `task.completed`
     - Android-клиент: заменить проверки `task.lastCompletedAt` на `task.completed`
     - Обновить отображение статуса выполнения задач
   - **Не удалять колонку `last_completed_at`**:
     - Использовать как служебное поле для расчёта интервалов; не экспонировать в API/клиенты
   - **Обновить историю задач** (если используется):
     - Если в `TaskHistory` хранится `last_completed_at`, обновить логику для работы с `completed`
     - Обновить метаданные истории, если они содержат `last_completed_at`
4) Добавить `enabled BOOLEAN NOT NULL DEFAULT TRUE` (если отсутствует) и миграционно выставить `TRUE`.
5) Обновить `recurrence_type` перечисление — добавить `weekdays`, `weekends`, `monthly_weekday`, `yearly_weekday`.

### Примеры
1) Частичное обновление `PATCH` (изменение только `reminder_time`):
```json
{
  "reminder_time": "2025-11-05T14:30:00"
}
```

2) Разовая задача (создание):
```json
{
  "title": "Разовая задача",
  "task_type": "one_time",
  "reminder_time": "2025-11-05T14:30:00"
}
```
Ответ (фрагмент):
```json
{
  "reminder_time": "2025-11-05T14:30:00",
  "readable_config": "14:30"
}
```

3) Еженедельная задача в понедельник 09:00:
```json
{
  "title": "Полить цветы",
  "task_type": "recurring",
  "recurrence_type": "weekly",
  "recurrence_interval": 1,
  "reminder_time": "2025-11-10T09:00:00",
  "anchor_weekday": 1
}
```

4) Будни (weekdays) в 08:00:
```json
{
  "title": "Зарядка",
  "task_type": "recurring",
  "recurrence_type": "weekly",
  "recurrence_interval": 1,
  "anchor_weekday": 8,
  "reminder_time": "2025-11-05T08:00:00"
}
```

5) Interval: выполнено не сегодня (расчёт от `last_completed_at`):
```json
{
  "title": "Полить сад",
  "task_type": "interval",
  "interval_days": 7,
  "reminder_time": "2025-11-05T10:00:00"
}
```
После `POST /tasks/{id}/complete` в 2025-11-07T18:45 новое `reminder_time` = 2025-11-14T10:00:00.

### Отладка и логирование
- Веб: `console.log` для сетевых запросов и событий WS.
- Android: `Log.d/e` в `TasksApi`, `MainActivity` (WS/HTTP), `ReminderScheduler`, `ReminderReceiver`, `ReminderActivity`.

### Изменения контракта
При изменении структуры/правил обновляйте:
- схемы Pydantic и сервисы форматирования сервера,
- клиентские парсеры и UI,
- данный документ.


