# Руководство пользователя Android приложения HomePlanner

Подробное описание всех функций и возможностей Android приложения HomePlanner.

## Содержание

1. [Обзор приложения](#обзор-приложения)
2. [Начало работы](#начало-работы)
3. [Вкладка "Сегодня"](#вкладка-сегодня)
4. [Вкладка "Все задачи"](#вкладка-все-задачи)
5. [Вкладка "Настройки"](#вкладка-настройки)
6. [Работа с задачами](#работа-с-задачами)
7. [Синхронизация данных](#синхронизация-данных)
8. [Уведомления](#уведомления)
9. [Офлайн режим](#офлайн-режим)
10. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

---

## Обзор приложения

HomePlanner — это Android приложение для управления домашними задачами и напоминаниями. Приложение работает в режиме offline-first, что означает, что оно полностью функционально даже без подключения к интернету.

### Основные возможности

- ✅ Просмотр задач на сегодня и всех задач
- ✅ Создание, редактирование и удаление задач
- ✅ Подтверждение выполнения задач
- ✅ Повторяющиеся задачи (ежедневные, еженедельные, ежемесячные и т.д.)
- ✅ Интервальные задачи
- ✅ Группировка задач
- ✅ Назначение задач пользователям
- ✅ Локальные уведомления о напоминаниях
- ✅ Автоматическая синхронизация с сервером
- ✅ Офлайн режим с автономной работой

### Интерфейс

Приложение состоит из трёх основных вкладок:

- **Сегодня** — показывает задачи, запланированные на сегодня (с учётом логического дня)
- **Все задачи** — показывает все доступные задачи
- **Настройки** — настройка подключения, выбор пользователя и другие параметры

---

## Начало работы

### Первый запуск

1. **Установка приложения**
   - Установите APK файл на ваше устройство
   - При первом запуске приложение запросит необходимые разрешения

2. **Запрос разрешений**
   - **Уведомления (Android 13+)**: Приложение запросит разрешение на показ уведомлений
   - **Точные будильники (Android 12+)**: Для работы напоминаний необходимо разрешение на использование точных будильников
   - Нажмите "Разрешить" для всех запросов

3. **Настройка подключения**
   - Перейдите на вкладку "Настройки"
   - Нажмите "Настроить подключение"
   - Введите параметры подключения к серверу:
     - **Хост**: IP адрес или домен сервера (для эмулятора: `10.0.2.2`)
     - **Порт**: Порт сервера (обычно `8000`)
     - **API Версия**: Версия API (обычно `0.2`)
     - **Использовать HTTPS**: Включите, если сервер использует HTTPS
   - Нажмите "Сохранить"

4. **Выбор пользователя**
   - В настройках нажмите "Выбрать пользователя"
   - Выберите вашего пользователя из списка
   - После выбора пользователя вы сможете просматривать задачи на вкладке "Сегодня"

### Настройка через QR-код

Вы можете настроить подключение через QR-код:

1. В настройках нажмите "Сканировать QR-код"
2. Отсканируйте QR-код с параметрами подключения
3. Настройки будут автоматически применены

**Формат JSON для QR-кода:**
```json
{
    "host": "10.0.2.2",
    "port": 8000,
    "api_version": "0.2",
    "use_https": false
}
```

---

## Вкладка "Сегодня"

Вкладка "Сегодня" показывает задачи, которые должны быть выполнены сегодня или просрочены.

### Фильтрация задач

Задачи фильтруются по следующим правилам:

- **Одноразовые задачи (one_time)**:
  - Видны, если время напоминания (`reminder_time`) сегодня или в прошлом (независимо от статуса выполнения)
  - Видны, если задача выполнена (`completed = true`), даже если время напоминания в будущем

- **Повторяющиеся задачи (recurring/interval)**:
  - Видны, если время напоминания сегодня или в прошлом (независимо от статуса выполнения)

### Логический день

Приложение использует понятие "логического дня", который начинается не в полночь, а в указанное время (по умолчанию 4:00). Это означает, что задачи, запланированные на раннее утро, считаются частью предыдущего дня.

**Пример:**
- Если логический день начинается в 4:00, то период с 4:00 до 3:59 следующего дня считается одним логическим днём
- Задача, запланированная на 3:30, будет считаться частью предыдущего дня

### Отображение задач

- Задачи сортируются по времени напоминания (хронологический порядок)
- Для просроченных задач показывается дата (например, "15.01")
- Для сегодняшних задач показывается время (например, "14:30")
- Задачи отображаются с названием группы (если назначена)
- Чекбокс показывает статус выполнения задачи

### Требования

Для просмотра задач на вкладке "Сегодня" необходимо:
- Выбрать пользователя в настройках
- Если пользователь не выбран, будет показано предупреждение: "⚠️ Выберите пользователя"

---

## Вкладка "Все задачи"

Вкладка "Все задачи" показывает все доступные задачи, независимо от даты и времени.

### Отображение задач

- Задачи сортируются по алфавиту (по названию)
- Всегда показывается время напоминания (формат: "HH:mm")
- Задачи отображаются с названием группы (если назначена)
- Чекбокс показывает статус выполнения задачи

### Активные и неактивные задачи

- **Активные задачи** (`active = true`) отображаются первыми
- **Неактивные задачи** (`active = false`) отображаются после активных (обычно это выполненные одноразовые задачи)

---

## Вкладка "Настройки"

Вкладка "Настройки" содержит все параметры конфигурации приложения.

### Информация о приложении

- **Версия приложения**: Показывает текущую версию и тип сборки (DEBUG/RELEASE)

### Строка состояния

Настройки отображения строки состояния (верхней панели):

- **Компактный режим**: Переключатель для включения/выключения компактного режима отображения
- В компактном режиме показываются только основные индикаторы
- В полном режиме показываются все индикаторы состояния

### Подключение к серверу

**Текущее подключение:**
- Показывает текущие параметры подключения (хост, порт, API версия, HTTPS)
- Кнопка "Изменить" для редактирования параметров
- Кнопка "Сканировать QR-код" для настройки через QR-код
- Кнопка "Проверить подключение" для тестирования соединения

**Настройка подключения:**

1. Нажмите "Изменить" или "Настроить подключение"
2. Введите параметры:
   - **Хост**: IP адрес или домен сервера
   - **Порт**: Порт сервера (по умолчанию 8000)
   - **API Версия**: Версия API (по умолчанию 0.2)
   - **Использовать HTTPS**: Включите для безопасного соединения
3. Нажмите "Сохранить"

**Проверка подключения:**

- Нажмите "Проверить подключение"
- Приложение проверит HTTP и WebSocket соединение
- Результаты теста будут отображены в диалоговом окне

### Выбор пользователя

**Текущий пользователь:**
- Показывает выбранного пользователя или "Пользователь не выбран"
- Кнопка "Выбрать пользователя" для выбора из списка

**Выбор пользователя:**

1. Нажмите "Выбрать пользователя"
2. Выберите пользователя из списка
3. Пользователь будет сохранён и использован для фильтрации задач

**Важно:** Выбор пользователя необходим для просмотра задач на вкладке "Сегодня".

### Хранилище

**Информация о хранилище:**

- **Использовано**: Процент использования хранилища (0-100%)
- **Задач в кэше**: Количество задач, сохранённых в локальном хранилище
- **Ожидающих синхронизации**: Количество операций в очереди синхронизации

**Очистка хранилища:**

- При необходимости можно очистить локальное хранилище (в будущих версиях)

---

## Работа с задачами

### Создание задачи

1. На вкладке "Сегодня" или "Все задачи" нажмите кнопку "+" (Floating Action Button) в правом нижнем углу
2. Заполните форму:
   - **Название**: Обязательное поле
   - **Описание**: Необязательное поле
   - **Тип задачи**: 
     - `one_time` — одноразовая задача
     - `recurring` — повторяющаяся задача
     - `interval` — интервальная задача
   - **Тип повторения**: Для повторяющихся задач (daily, weekly, monthly, yearly и т.д.)
   - **Интервал повторения**: Для повторяющихся задач (каждые N периодов)
   - **Интервал в днях**: Для интервальных задач
   - **Время напоминания**: Дата и время напоминания (обязательное поле)
   - **Группа**: Выбор группы для задачи (необязательно)
   - **Назначенные пользователи**: Выбор пользователей для задачи
3. Нажмите "Создать"
4. Задача будет создана локально и отправлена на сервер в фоне

### Редактирование задачи

1. На вкладке "Сегодня" или "Все задачи" свайпните задачу вправо (или влево)
2. Откроется форма редактирования с предзаполненными данными
3. Измените нужные поля
4. Нажмите "Сохранить"
5. Изменения будут применены локально и отправлены на сервер в фоне

### Удаление задачи

1. На вкладке "Сегодня" или "Все задачи" свайпните задачу влево
2. Появится диалог подтверждения удаления
3. Нажмите "Удалить" для подтверждения
4. Задача будет удалена локально и отправлена на сервер в фоне

**Внимание:** Удаление задачи нельзя отменить!

### Подтверждение выполнения задачи

1. На вкладке "Сегодня" или "Все задачи" нажмите на чекбокс задачи
2. Задача будет отмечена как выполненная
3. Изменение будет применено локально и отправлено на сервер в фоне

### Отмена подтверждения

1. Нажмите на чекбокс выполненной задачи ещё раз
2. Задача будет отмечена как невыполненная
3. Изменение будет применено локально и отправлено на сервер в фоне

### Быстрые действия

При создании или редактировании задачи доступны быстрые действия:

- **Ежедневно 9:00**: Создаёт ежедневную задачу на 9:00
- **Еженедельно**: Создаёт еженедельную задачу

---

## Синхронизация данных

### Автоматическая синхронизация

Приложение автоматически синхронизирует данные с сервером:

- При наличии интернет-соединения операции из очереди отправляются на сервер
- После синхронизации локальный кэш обновляется данными с сервера
- Синхронизация происходит в фоне, не блокируя работу приложения

### Ручная синхронизация

Вы можете запустить синхронизацию вручную:

1. Нажмите на индикатор синхронизации в строке состояния (верхняя панель)
2. Или нажмите на количество ожидающих операций в строке состояния
3. Синхронизация начнётся немедленно

### Статус соединения

Строка состояния показывает статус соединения с сервером:

- 🟢 **ONLINE** (зелёная иконка): Соединение активно, последняя попытка была успешной
- 🟡 **DEGRADED** (жёлтая иконка): Есть неуспешные попытки, но меньше 5 подряд
- 🔴 **OFFLINE** (красная иконка): 5 неуспешных попыток подряд или превышено время с последней успешной попытки
- ⚪ **UNKNOWN** (серая иконка): Начальное состояние при запуске, ещё не было попыток подключения

### Очередь операций

Все операции (создание, редактирование, удаление, подтверждение) добавляются в очередь синхронизации:

- Операции выполняются локально немедленно (оптимистичные обновления)
- Операции отправляются на сервер в фоне
- Если операция не удалась, она остаётся в очереди и будет повторена при следующей синхронизации

---

## Уведомления

### Локальные уведомления

Приложение использует локальные уведомления для напоминаний о задачах:

- Уведомления срабатывают в указанное время (`reminder_time`)
- Уведомления работают даже в офлайн режиме
- Для работы уведомлений необходимо разрешение на показ уведомлений (Android 13+)

### Разрешения для уведомлений

**Android 13+ (API 33+):**
- При первом запуске приложение запросит разрешение на показ уведомлений
- Разрешение можно изменить в настройках системы

**Android 12+ (API 31+):**
- Для точных уведомлений необходимо разрешение на использование точных будильников
- Приложение покажет диалог с объяснением и кнопкой для открытия настроек системы

### Тестирование уведомлений

В настройках можно протестировать уведомление для задачи (функция доступна в будущих версиях).

---

## Офлайн режим

### Работа без интернета

Приложение полностью функционально в офлайн режиме:

- ✅ Просмотр задач из локального кэша
- ✅ Создание новых задач
- ✅ Редактирование задач
- ✅ Удаление задач
- ✅ Подтверждение выполнения задач
- ✅ Локальные уведомления
- ✅ Пересчёт дат для повторяющихся задач

### Автоматическая синхронизация при восстановлении соединения

Когда интернет-соединение восстанавливается:

- Приложение автоматически синхронизирует очередь операций с сервером
- Локальный кэш обновляется данными с сервера
- Все изменения, сделанные в офлайн режиме, отправляются на сервер

### Локальное хранилище

Приложение хранит все данные локально в Room Database:

- Задачи кэшируются в локальной базе данных
- Все операции сохраняются в очереди синхронизации
- Данные доступны даже после перезапуска приложения

---

## Часто задаваемые вопросы

### Как настроить подключение к серверу?

1. Перейдите на вкладку "Настройки"
2. Нажмите "Настроить подключение"
3. Введите хост, порт, API версию и выберите использование HTTPS
4. Нажмите "Сохранить"

Для эмулятора используйте хост `10.0.2.2` вместо `localhost`.

### Почему вкладка "Сегодня" пустая?

Вкладка "Сегодня" требует выбора пользователя:

1. Перейдите на вкладку "Настройки"
2. Нажмите "Выбрать пользователя"
3. Выберите вашего пользователя из списка

После выбора пользователя задачи появятся на вкладке "Сегодня".

### Как работают повторяющиеся задачи?

Повторяющиеся задачи автоматически пересчитывают дату напоминания:

- При наступлении нового логического дня выполненные задачи получают новую дату
- Дата рассчитывается на основе типа повторения (daily, weekly, monthly и т.д.)
- Невыполненные задачи сохраняют свою дату до выполнения

### Как работает логический день?

Логический день начинается не в полночь, а в указанное время (по умолчанию 4:00):

- Период с 4:00 до 3:59 следующего дня считается одним логическим днём
- Это позволяет считать задачи раннего утра частью предыдущего дня

### Почему уведомления не приходят?

Убедитесь, что:

1. Разрешено показ уведомлений (Android 13+)
2. Разрешено использование точных будильников (Android 12+)
3. Задача имеет установленное время напоминания (`reminder_time`)
4. Время напоминания ещё не прошло

### Как синхронизировать данные вручную?

Нажмите на индикатор синхронизации или количество ожидающих операций в строке состояния (верхняя панель).

### Что означает статус соединения в строке состояния?

- 🟢 **ONLINE**: Соединение активно
- 🟡 **DEGRADED**: Есть проблемы с соединением, но оно частично работает
- 🔴 **OFFLINE**: Нет соединения с сервером
- ⚪ **UNKNOWN**: Статус ещё не определён (начальное состояние)

### Как очистить локальное хранилище?

Функция очистки хранилища будет доступна в будущих версиях.

---

## Дополнительная информация

### Поддержка

При возникновении проблем:

1. Проверьте настройки подключения
2. Проверьте статус соединения в строке состояния
3. Попробуйте перезапустить приложение
4. Обратитесь к документации проекта

### Версия приложения

Версия приложения отображается на вкладке "Настройки" в разделе "Версия приложения".

---

**Последнее обновление:** 2025-12-27

