# История задач

В системе реализована полная история изменений задач.

## Возможности

История задач хранит:
- Время создания задачи
- Время первого показа каждой итерации задачи
- Время подтверждения выполнения
- Время отмены подтверждения
- Все изменения задачи (редактирование полей)
- Активацию и деактивацию задачи
- Удаление задачи

## Модель данных

Таблица `task_history` содержит:
- `task_id` - ID задачи
- `action` - тип действия (CREATED, FIRST_SHOWN, CONFIRMED, UNCONFIRMED, EDITED, DELETED, ACTIVATED, DEACTIVATED)
- `action_timestamp` - время действия
- `iteration_date` - дата итерации задачи (next_due_date на момент действия)
- `meta_data` - JSON с дополнительными данными (старые/новые значения при редактировании)

В таблице `tasks` добавлено:
- `last_shown_at` - время последнего показа текущей итерации

## API

### Получить историю задачи

```http
GET /api/v0.2/tasks/{task_id}/history
```

Возвращает список всех записей истории для задачи, отсортированных по времени (новые сначала).

### Отметить задачу как показанную

```http
POST /api/v0.2/tasks/{task_id}/mark-shown
```

Обновляет `last_shown_at` и логирует `FIRST_SHOWN` если это первая демонстрация данной итерации.

## Интерфейс

В левом меню добавлена кнопка "📜 История задач" для просмотра истории всех задач.

История отображается отдельным видом с фильтрами и показывает:
- **Дата и время** действия в локальном часовом поясе (чёрным цветом)
- **Название задачи с группой** (в формате "Группа: Задача")
- **Тип действия** (цветным для улучшения читаемости)
- **Изменения** (для EDITED - показывает что на что поменялось)

Цвета действий:
- 🟢 Создана/Активирована - зелёный
- 🔵 Показана - синий
- 🟢 Подтверждена - светло-зелёный
- 🟠 Отмена - оранжевый
- 🟡 Изменена - жёлтый
- 🔴 Удалена - красный
- ⚫ Деактивирована - серый

История сортируется по времени от новых к старым. Доступны фильтры по группе, задаче и периоду времени.

## Автоматическое логирование

Система автоматически логирует:
- **CREATED** - при создании задачи (с полными данными создания)
- **FIRST_SHOWN** - при первом показе итерации задачи в представлении "Сегодня"
- **CONFIRMED** - при подтверждении выполнения (с указанием итерации)
- **EDITED** - при изменении любого поля (с указанием что на что изменилось)
- **UNCONFIRMED** - при отмене подтверждения
- **DELETED** - при удалении задачи (с указанием настроек задачи)

Логирование первого показа происходит автоматически при отображении задачи в представлении "Сегодня". Система использует поле `last_shown_at` для отслеживания, была ли уже показана текущая итерация, и не создаёт дубликаты записей.

## Админ режим

В левом меню доступна кнопка "🔧 Админ режим" для переключения в режим администратора. В этом режиме доступны расширенные функции:
- **Удаление записей истории** - кнопка удаления появляется рядом с каждой записью в истории

### Удаление записи истории

```http
DELETE /api/v0.2/history/{history_id}
```

Удаляет указанную запись истории. Доступно только в режиме администратора.

## Часовой пояс

Все времена в истории отображаются в локальном часовом поясе. Часовой пояс задаётся вручную в секции `[app]` файла `config/settings.toml` (поле `timezone`) — автоматического определения больше нет.

## Миграция

Таблица создастся автоматически при первом запуске сервера. Поле `last_shown_at` было добавлено в существующую таблицу `tasks` вручную.

При удалении задачи все записи её истории сохраняются. Поле `task_id` в таблице `task_history` устанавливается в `NULL` (ON DELETE SET NULL), что позволяет сохранить историю даже после удаления задачи.
